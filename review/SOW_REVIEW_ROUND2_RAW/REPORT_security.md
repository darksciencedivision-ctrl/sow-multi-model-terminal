# REPORT_security — SOW independent review (area: security)
Repo: D:\multi model terminal app\sovereign-orchestration-workspace @ bad029e7 (read-only). Started 2026-08-16.

## Running log (appended incrementally)

### STT confidence semantics — [OBSERVED] CONFIRM prior claim
- adapters/voice_parakeet/wsl_parakeet.py:202-216 `WSL_TRANSCRIBE_SRC` prints ONLY `{"text": ...}` (line 213) — no `confidence` key is ever emitted by the real engine.
- wsl_parakeet.py:189-193 `parse_transcript`: no in-range JSON confidence ⇒ `confidence = 1.0 if text.strip() else 0.0`. So on the real NeMo path every non-empty transcript is 1.0; the only 0.0 case is an empty string.
- Consequence: command_broker.py:123 (`cmd.confidence < self._threshold`) and conductor_voice.py:221 (`confidence < self._threshold`) never fire on a real mis-hearing; the CLARIFY-on-low-confidence branch is dead for real audio. The docstring at 176-181 discloses this as policy ("U140 uncalibrated"). Verdict: CONFIRM (the "confidence" gate is a mock-only control; on the real path the gate is effectively text-nonempty).
- Also: the WSL runner passes `input_text=WSL_TRANSCRIBE_SRC` and `model_id` from env `SOW_PARAKEET_MODEL` (wsl_parakeet.py:128); `ASRModel.from_pretrained` (line 207) will fetch from HF/NGC if not cached — WSL-side network egress despite module docstring "no network from this module" (INFO).

### Protected-verb lexical matcher — [OBSERVED]
- voice_bridge/spoken_verbs.py:54 tokenizes `[A-Za-z0-9_-]+` on lowercased text; :57 maps each token through VOICE_VERB_SYNONYMS (:20-38); :58 picks the FIRST token in GATED_VERBS else index 0.
- GATED = PROTECTED (spawn, spawn_node, grant, grant_permission, approve, promote, elevate, authorize) ∪ DESTRUCTIVE (terminate, kill, delete, remove, merge, shutdown, purge) — command_broker.py:25-28.
- Paraphrases that BYPASS (route to CHAT, delivered verbatim to the live conductor CLI per conductor_voice.py:236-240): "get rid of", "trash", "drop", "nuke", "clear out", "clean up", "rm -rf", "unlink", "eliminate", "obliterate", "discard", "scrap", "roll back", "revert", "overwrite", "truncate", "abort", "cancel", "halt", "end", "close", "quit", "exit" (destructive class); "launch", "run", "open", "boot", "instantiate", "provision", "fork", "add a node", "bring up" (spawn class); "permit", "let it", "enable", "unlock", "escalate", "sudo", "sign off", "green-light", "go ahead", "yes do it", "confirm", "accept", "ok", "bless" (approve/grant class). Nouns/inflections not in table: "deletion", "removal", "termination", "merger", "approval", "authorization", "purging"→ covered; "kill" covered; "shut down" → "shut"→"shutdown" covered.
- Hyphenated tokens defeat the table because `-` is inside the token class: "re-move", "de-lete", "auto-approve" are single tokens not in the table (spoken_verbs.py:54). Unicode look-alikes (Cyrillic е) also miss (regex is ASCII-only).
- Precondition: the gate only matters if CHAT to the conductor could cause action; the design's real control is the per-turn hook profile (turn-authority.js) — see below.

### turn-authority.js server bounds — [OBSERVED]
- Bind: `net.createServer` on 127.0.0.1 port 0 exclusive (turn-authority.js:671), verifies bound addr is 127.0.0.1 (:674). Token 32 random bytes hex (:169), timingSafeEqual (:149-153, :406). Request cap MAX_REQUEST_BYTES 64 KiB (:115, :649). One request per connection (`socket.end` in reply :645). Schema pinned (:657). Fail-closed reply on parse error (:660).
- Gaps: no per-socket idle timeout and no connection cap — a client that connects and sends <64 KiB without `\n` holds a socket + up to 64 KiB buffer indefinitely (:647-654); `_sockets` grows unbounded until stop(). Loopback + same-user only, so LOW.
- Token travels to the supervised claude CLI as env `SOW_VOICE_AUTH_TOKEN` (:622-631; main.js:917) — applied AFTER `scrubbedLaunchEnv` (main.js:902), so the scrub (which matches substring TOKEN) does not strip it; then `sovereignControl.childEnv` adds SOVEREIGN_CONTROL_TOKEN (main.js:924-930). Order is correct; noted because `env-scrub.childEnv()` (env-scrub.js:577-585) would REFUSE `SOW_VOICE_AUTH_TOKEN` as a credential name — it is not used on this path, so no break.
- Any same-user process that can read the child's env (or that IS the vendor CLI) can drive hook events; by design only deny/observe outcomes are reachable except the U178 `ended` release on an unmarked UserPromptSubmit (:506-516) — that release is explicitly disclosed as the "safe direction".

### Electron control gateway (apps/desktop/control/sovereign-control-server.js) — [OBSERVED]
- http.createServer bound 127.0.0.1:0 (:186-194); only `POST /v1/tools/call` (:277); Bearer token base64url(32 B) (:72-76, :99); MAX_BODY 1 MiB (:6, :338-357). No Origin/Host header check, no CORS (so browsers cannot send Authorization cross-origin without preflight — preflight gets 404 → blocked). Token lookup is a `Map.get` (:118) — not constant-time; irrelevant on loopback.
- `_recordOperation` (:163-171) retains the last 200 `arguments` payloads per node in memory and hands each to `_onOperation` — check what main.js does with them (below).
- Port + token persistence: issued in-memory only; `childEnv` (:173-178) puts SOVEREIGN_CONTROL_PORT/TOKEN/STORE_ROOT into the child env. `.sovereign_store` path (REPO_ROOT/.sovereign_store) is disclosed to the child.

### IPC_TOKEN / IPC_KEY exposure — [OBSERVED]
- control_plane/ipc/run_gateway.py:75-76 prints `IPC_TOKEN=`/`IPC_KEY=` to STDOUT. Reader: apps/desktop/main.js:437-455 `resolveGateway` spawns `py -3.12 -m control_plane.ipc.run_gateway` with `{...process.env, SOVEREIGN_IPC_NODE/ROLE/PROJECT}` and parses stdout in memory (regex :448). Only STDERR is written to the main-process log (`log(\`[gateway] ...\`)` :456); stdout is not logged. main.js:2292 logs only port. ⇒ the token/key do NOT land in main-process.log on the normal path. [OBSERVED]
- Residual: (a) if the operator runs `run_gateway` manually in a console the values are echoed on screen/scrollback (design); (b) `IPC_TOKEN`/`IPC_KEY` honoured from `process.env` (main.js:427-430) — `IPC_TOKEN` matches the scrub substring TOKEN, but `IPC_KEY` matches NO rule in env-scrub.js:533-540 / claude_code.py, so if the operator supplies the credential via env, the HMAC key half is inherited by every governed child. Precondition: env-supplied gateway (not the default spawn path). LOW.
- run_gateway.py:67-72 fixed-credential path needs IPC_ALLOW_FIXED_CRED=1 + IPC_FIXED_TOKEN/KEY (env). Values are read, never printed except via the same stdout lines.

### Hook transport fail-open edge — [INFERRED] (new)
- The pinned Claude hook is `node tools/live/voice_turn_boundary.js` (node_runtime/supervisor/conductor_permission_profile.py:128-146, settings via `--settings <inline JSON>` at tools/live/emit_conductor_launch.py:185). The hook script `require`s `../../apps/desktop/voice/turn-authority` at module top (tools/live/voice_turn_boundary.js:11-15). Fail-closed answers are JSON+exit 0 (`permissionDecision: deny`) via failClosedHookResult (turn-authority.js:125-147); non-decision events use exit 2.
- If the hook PROCESS itself dies before writing (e.g. `require` throws because apps/desktop/voice/turn-authority.js or the hook file was rewritten to non-JS during an UNRESTRICTED typed turn, or Node OOM), Claude Code's hook contract treats a non-2 non-zero exit as a NON-BLOCKING error and the tool call proceeds. The U181 policy-byte hash check (main.js:328-339, 912-916) only runs inside `handle()` — i.e. only when the transport reaches main — so a crash-before-connect is invisible to it. Precondition: same-user write to the repo during a typed turn (the vendor CLI has that; its cwd is REPO_ROOT). Severity LOW–MEDIUM; the doc header already concedes "depends on the first-party CLI honoring its pinned hook contract" (turn-authority.js:12-13). Fix shape: wrap the hook's top-level require in try/catch and exit 2 with a stderr reason on any failure; and/or verify hook bytes on the launch-side before each spoken delivery.
- Marker prefix `[[SOVEREIGN_VOICE_CHAT_V2:<hex>]] ` (turn-authority.js:114,159-161) is written BEFORE the transcript, so a transcript beginning with `!` (Claude REPL bash mode) or `/` (slash command) cannot be interpreted as such — good. Interior CR/LF are collapsed (conductor-write.js:116-118). Other control bytes (\x03, \x1b, \t) in a transcript are NOT stripped — theoretical only (NeMo output).

### Transcript / audio persistence (invariant 26) — [OBSERVED]
- Audio IS written to disk transiently: apps/desktop/voice/capture-store.js:361-369 writes `apps/desktop/.voice-captures/capture-<uuid>-<ts>-<seq>.wav` (`wx`, mode 0o600 — mode is a no-op on Windows/NTFS: no ACL is set, so any same-user process can read it during the transcription window). Discard in `finally` (:410-419); janitor purges files older than 10 min (:262, :469-495) every 60 s (:453-461). So the residency window is the transcription (≤180 s + 30 s) and, after a crash, ≤10 min + janitor interval. Disclosed in the header (:219-239). CONFIRM "audio touches disk"; NARROW: bounded and cleaned, but no ACL, and `.voice-captures` is a repo-relative dir shared across instances.
- Transcripts: the shell records session events (apps/desktop/approvals/session-events.js) which carry `outcome.text` — need to confirm whether the spoken TEXT persists under `.approvals/` (see below).

### Transcript persistence — [OBSERVED]
- apps/desktop/approvals/session-events.js:47 default log `apps/desktop/.approvals/session-events.jsonl`; `recordUtterance` (:121-140) writes the spoken TEXT + confidence + classification for non-CHAT outcomes (proposed_action / clarify) via appendFileSync (:110). Truncated at launch (:83), capped by maxEvents. CHAT utterances are NOT recorded there (:127 "chat ⇒ nothing to approve"). No TTL — lives until the next launch truncates. Gitignored (.gitignore line "apps/desktop/.approvals/"). Present on host: apps/desktop/.approvals/session-events.jsonl (0 bytes) + selfcheck/. Verdict: transcripts of NON-chat utterances persist across a session, chat ones do not; disclosed in the file header (:35).

### Operator PII in tracked files — [OBSERVED]
- `git grep -c "Sslaw"`: 27 tracked files, 380 occurrences. By area: tools/soak/results/*.jsonl 321 (5 PHASE2_SOAK event logs carrying absolute `C:\Users\Sslaw\...` paths), docs/evidence 56 (reports + receipts, incl. docs/evidence/live/phase18a_host_recon.json 4, PHASE18E receipts 4 each), docs/loop/LOOP_STATE.json 2, docs/registers/DECISION_REGISTER.md 1.
- Machine name `DESKTOP-03PTABH`: 1 tracked file (docs/evidence/PHASE14D_EVIDENCE_REPORT.md). Email: 0 tracked hits for darksciencedivision/@gmail.
- Severity LOW (local-only repo; but the operator has published sibling repos publicly before — these paths would ship in any publication of this tree).

### .gitignore coverage — [OBSERVED]
- Covered: `.env`, `.env.*`, `*.log`, `*.sqlite*`, `.sovereign_store/`, `apps/desktop/.recovery/`, `apps/desktop/.voice-captures/`, `apps/desktop/.approvals/`, `config/live_operation.json`, `docs/loop/logs/`, `secrets/`.
- NOT covered by pattern: `*.db` — but the only .db files (`.sovereign_store/sovereign.db` + `.sovereign_store/tmp-tests/**`) sit under an ignored dir, so nothing leaks today. `.sovereign_store/tmp-tests/pytest-of-Sslaw/` shows the test suite writes pytest tmp under the repo store (harmless, ignored).
- Tracked-file scan `git ls-files | grep -iE "token|secret|key|\.env|\.db|approvals|voice-captures|recovery|\.log$"`: no credential-bearing files; matches are source (`token_meter.py`, `session_approvals.py`, recovery modules), `config/live_operation.example.json`, and 5 `tools/soak/results/*_events.jsonl` (PII paths only). No `.env`, `.db`, `.log`, or real `live_operation.json` tracked. `config/live_operation.json` present on host, ignored; keys: _comment, config_version, live_operation_authorized, register_row, scope, conductor (no secret values by key name).

### Loopback listener inventory — [OBSERVED]
| Listener | Bind | Auth | Origin/Host check | Body/req limit | Idle timeout | Persistence |
|---|---|---|---|---|---|---|
| Electron control gateway (apps/desktop/control/sovereign-control-server.js:186-194) | http 127.0.0.1:0 | Bearer base64url(32B), Map lookup :118 | none (no CORS; browsers cannot attach Authorization cross-origin) | MAX_BODY 1 MiB :6,:344 | Node http default (keep-alive; `closeAllConnections` at stop :260) | in-memory only; token+port to child env :173-178 |
| Voice turn authority (apps/desktop/voice/turn-authority.js:636-682) | net 127.0.0.1:0 exclusive | 64-hex token, timingSafeEqual :149-153 | n/a (raw JSON line) | 64 KiB :115 | NONE (holds sockets until stop) | in-memory; token to child env `SOW_VOICE_AUTH_TOKEN` :622-631 |
| Python IPC gateway (control_plane/ipc/gateway.py:249-264, wsframe.py) | ThreadingTCPServer 127.0.0.1:0, `allow_reuse_address=True` :257 (Windows SO_REUSEADDR ⇒ a same-user process may co-bind the port) | per-node token (`_by_token.get`, :66-68, not constant-time) + per-envelope HMAC-SHA256 with `hmac.compare_digest` (envelope.py:42-47) + from_node binding :223 | WS handshake checks only Upgrade/Sec-WebSocket-Key (wsframe.py:222-243) — NO Origin check; mitigated by token+HMAC | MAX_MESSAGE 8 MiB (wsframe.py:32,123,164); handshake head 16 KiB :202 | none visible | token/key printed to stdout once (run_gateway.py:75-76); optional fixed cred via env only with IPC_ALLOW_FIXED_CRED=1 :67-72 |
| MCP memory server (mcp_server/server.py:83-110) | TCP 127.0.0.1:0 line-JSON | bearer `token` per request, dict `.get` (auth.py:27-29) | n/a | max_line 8 MiB :86 | handler timeout 60 s :89 | BOOTSTRAP_TOKEN printed to stdout when SOVEREIGN_BOOTSTRAP_ROLE set (run_server.py:37) |
| MCP stdio (mcp_server/sovereign_tools.py) | stdio (no socket) | inherits SOVEREIGN_CONTROL_TOKEN from env → HTTP to Electron gateway | — | — | — | — |
- No replay protection on the IPC envelope (no nonce/ts check in gateway.py:208-227) — a captured envelope could be replayed by a same-user peer; loopback so INFO.
- All five bind loopback only; none uses a fixed port. Threat boundary is uniformly "same Windows user" (as docs/THREAT_MODEL.md itself scopes). Nothing found that a non-local or other-user principal could reach.

### Path containment (node_runtime/workspace/binding.py `WorkspaceBinding.resolve`) — [OBSERVED, executed on host py 3.12.10 / Win11 26200]
Probe: scratchpad/security/probe_paths.py (real function, real junction, real 8.3 name). Results:
- REFUSED: `file.txt:stream`, `file.txt::$DATA`, `C:foo` (':' rule :63-64); `C:\foo`, `\?\C:\foo`, `\.\C:\foo`, `\?\<root>\x`, `\server\share\..\..\x`, `//server/share/x`, `\?\UNC\...` (is_absolute :60-61); `foo.`, `foo `, `foo. .` and EVERY `..` form (`..`, `../x`, `sub/../..`, `sub\..\..\outside\secret.txt`) — note `..` is caught by the trailing-dot rule at :65 (`"..".rstrip(" .") == ""`), not by the resolve check; `CON`, `con.txt`, `Con.TXT`, `PRN`, `AUX`, `NUL`, `COM1..9`, `LPT1..9`, `com1.a.b`, `nul.tar.gz` (:67-68); `a<b a>b a"b a|b a?b a*b ? *.txt` (:65); `/foo` and `\foo` (rooted-no-drive: pathlib joins as `D:\foo` → caught by the parents check :69-71); `junc/secret.txt` (junction to outside → resolve() follows it → refused :69-71).
- ACCEPTED (inside root, benign): 8.3 short name `LONGDI~1/x.txt` (resolves to the long name inside root), case variants, NFC/NFD `é.txt` (two distinct files, no escape), `foo\xa0`, `sub//x`, `./sub/x`, `""`/`.` (→ root itself), `slink/secret.txt` (symlink creation refused on this host without privilege → non-existent path, stays inside).
- ACCEPTED but device-name screen GAPS: `con .txt`, `nul .txt` (space before the extension — RtlIsDosDeviceName_U trims spaces before `.` on legacy Windows, the screen's `part.split(".")[0]` keeps the space, :67), `COM¹`/`COM²`/`COM³` (superscript digits, documented reserved on Win10), `COM0`, `COM10`, `LPT0`, `CONIN$`, `CONOUT$`, `CLOCK$`. Baseline on THIS host (Win11 26200): CreateFile with a directory prefix materialises regular files for CON/COM1/con.txt/`con .txt`/COM¹ (only bare `NUL` still maps to the device) — so on this build none of these is a fail-open; on Windows 10 hosts `nul .txt`/`COM¹` would be. Severity LOW; fix shape: normalise `part.split(".")[0].rstrip(" ")` and add `com[¹²³]`.
- No traversal bypass found. Only ONE production caller: node_runtime/workspace/worktree.py:95 — the vendor CLIs (claude/codex/…) never pass through this API (binding.py:8-9 says so; U10/U25 owed).
- `x\x00y` raises ValueError from stat (uncaught → not a WorkspaceEscape; callers must catch generic exceptions).
