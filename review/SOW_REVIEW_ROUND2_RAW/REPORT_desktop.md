# REPORT_desktop (incremental)
Area: apps/desktop/ — reviewer: desktop. HEAD bad029e7. Started 2026-08-16.

## Working notes (main.js read fully 1-2905; preload/logger/supervisor/recovery-store/ipc read)

- main.js:313-317 SOW_MAIN_LOG_FILE used raw as logger file; logger mkdirSync(dirname)+appendFileSync per line (main-process-logger.js:134,148); rendererSink -> win.webContents.send("shell:log", line) for EVERY line (main.js:315-317, logger.js:200). No rotation anywhere in logger. => R-56 CONFIRM.
- main.js:426-460 resolveGateway: token/key parsed from gateway stdout; stderr lines logged verbatim `[gateway] ...` (456); stdout never logged. Values held in IpcClient (_token/_key) memory only; envelope carries token in auth.node_credential over ws://127.0.0.1 (ipc/client.js:550-552, envelope.js:671). Not logged in desktop code [OBSERVED]. Gateway env = full process.env + role shell (439).
- main.js:469-485 ptyFactory: `spec.env || {...process.env}` -> a renderer `pane:new` pane inherits FULL Electron env. main.js:1467 pane:new -> sanitizeRendererSpec strips only env/cwd (488-496); `spec.file`, `spec.args`, `spec.title`, cols/rows pass through to pty.spawn (476). => renderer can name ANY executable+args. (finding N-1)
- main.js:498 `let paneSeq = 0`; 502/612/1906 `pane-${++paneSeq}` => R-45 premise CONFIRMED in main; renderer side to check.
- main.js:1468-1482 pane:input: residue clear when id===conductorPaneId && operatorInputResolvesResidue(data); comment 1469-1475 says channel is NOT operator provenance. => R-44 CONFIRM (need to weigh impact: residue clear only re-enables voice delivery; the voice path still requires paneAcceptsTypedText+echo).
- main.js:1521 pane:close -> manager.kill(any id) incl conductor pane; 1543 conductor:launch; 1533 conductor:select; 1846 pane:spawnFromSelection; 1558 approvals:decide -> routeApprovalDecision (Python). All renderer-reachable.
- main.js:1544-1549 conductor:succeed only logs + returns affordance => Rev-2 CONFIRM. approvals:decide 1558-1582: routes to Python resolve, records event; "nothing executed" (1573-1577) => Rev-2 CONFIRM.
- main.js:2255 killPane: (paneId) => manager.kill(paneId) bound into applicationControl; stop_worker path to check in control/application-control.js (R-17).
- main.js:2347-2349 recoveryStore.persist(manager.registry.eventLog()) on every spawn/exit/kill/refused => R-39 premise; need session-registry.js.
- Quit path: completeNormalQuit 2565-2601: manager.shutdown(15000), waitForSessionReleases(15000), conductorVoiceAuthority.stop() (check bounded), sovereignControl.stop() (U334 bounded), waitForChildExit(15000), mainProcessLogger.flushAndDetach() -> awaits pendingWrites settle with NO timeout (logger.js:204-209) — a stdout write whose callback never fires (blocked pipe reader, not EPIPE) hangs quit [INFERRED]. (finding N-Q1)
- main.js:2413-2415 before-input-event -> ANY typed key disarms voice turn (design U166).
- main.js:2442-2445 permission handlers: audio only. Good.
- Log lines include argv (976), reasons from Python, picker option labels (2007: `${o.provider}/${o.label}`) -> renderer log via shell:log; check renderer for innerHTML use.

### Adjudications so far (renderer + terminal read)
- R-39 NARROW: session-registry.js:32 `_log=[]`, :105-108 append per transition; :103 eventLog() shallow-copies whole log; main.js:2347-2349 persists on spawn/exit/kill/refused only. Growth is per LIFECYCLE event (not per data byte) and the on-disk file is overwritten (RecoveryStore.persist -> {log: eventLog}) so it is per-process. O(n^2) copy over n lifecycle events; n is small in practice. Real but LOW.
- R-71 CONFIRM (as stated): session-manager.js:154-161 write() passes `data` to handle.pty.write with no type/size check; resize bounds live in panes/resize-intent.js:39-43 (1..32767 ints). pane:input (main.js:1468) passes renderer `data` verbatim -> non-string throws inside node-pty (rejected invoke, no crash) [INFERRED].
- R-72 NARROW: node-pty 1.1.0 windowsPtyAgent.js:72 `_outSocket.setEncoding('utf8')` => onData delivers decoded STRINGS; registry.feed makes one Buffer per chunk (ring-buffer.js:47), so chunk boundaries are char-aligned. Only the single-oversized-chunk trim (ring-buffer.js:60-63, chunk > 256 KB) cuts at an arbitrary byte. main.js:2324 `event.data.toString("utf8")` is a no-op on a string. snapshot() = Buffer.concat per reattach/paneEmittedAll — allocation only.
- R-46 CONFIRM: pane-wiring.js:141-160 refuseSelection reads sel.option (renderer object) — `available`, `roles`, `provider/label` all renderer-supplied; conductor path main.js:1438-1446 re-finds by (adapter, model_slug, label) in lastPickerModel and re-checks host flags. Worker path: need worker-spawn.js/launch-source.js to see whether Python re-verifies host enumeration (comment at main.js:1824 says "host-enumeration verification").
- Rev-2 conductor:succeed CONFIRM main.js:1544-1549 (log + return affordance, nothing else). approvals:decide main.js:1558-1582 -> routeApprovalDecision (Python resolve) + recordGovernedDecision; outcome string 1577 says "nothing executed".
- NEW candidate N-2 (renderer XSS-shape): renderer.js:456-463 rail.innerHTML interpolates `${info.title || c.id}` and `${c.id}` UNESCAPED; renderer.js:499-501 minimized bar `${m.title || m.id}` UNESCAPED. `esc()` exists (607) and is used everywhere else. Titles: pane:new spec.title (renderer-controlled, low value), conductor `CONDUCTOR · ${display_name}` from Python selection feed, worker `spec.title` from ticket (pane-wiring.js:118). CSP index.html:5 `script-src 'self'` blocks inline script; impact = DOM/UI spoofing (fake READY/state text) [INFERRED].

- R-17 CONFIRM: application-control.js:237-244 stopWorker = requireControlRole(conductor) + deadlines.clear + io.killPane(rec.paneId) (main.js:2255 => manager.kill). No Python call. assign_task 265-283: preflightAssignment (JS assignmentRefusal) then io.writePanePrompt — JS-only. notify_message/notify_debate/notify_debate_turn (285-324): NO requireControlRole at all — only `sender_node_id === identity.node_id`; any node role (worker) can notify any recipient incl. the conductor's pane (pane-writer.js:327-330 routes nodeId==conductor to pane 1).
- NEW N-3 (MEDIUM): system->pane bodies are written RAW with no control-character filter. pane-writer.js:273 `io.write(paneId, prompt)` -> main.js:2127 manager.write -> session-manager.js:160 pty.write. Bodies interpolate caller/model text: application-control.js:302-303 (message_id, task_id, sender), :311-312 (debate.proposition), :320 (args.debate_id — raw caller string), :23-34 (task objective/scope/constraints/acceptance). A `\r` inside the body is an Enter that the U328 gate never sees (the gate checks the screen BEFORE the body and BEFORE its own `\r`, pane-writer.js:268,278,305), and `\x1b` sequences are terminal control (bracketed-paste end `\x1b[201~`, OSC title, etc.). Reachability: the node's bearer token is in the CLI child env (sovereign-control-server.js:173-178 childEnv) so ANY descendant process of a worker CLI (i.e. a model-run shell command) can POST /v1/tools/call directly (277-335), bypassing Python's shape checks (collaboration_service.py mints message_id itself, but the JS handler does not require that) — `notify_message` with a crafted `message.message_id` reaches the conductor pane; `notify_debate_turn` uses `args.debate_id` raw. Precondition: a live worker node whose model (or anything with its token) is adversarial/prompt-injected. Fix shape: reject bodies matching /[\x00-\x08\x0b-\x1f\x7f]/ (or strip CR/ESC) in writePrompt, and validate id fields (`^[A-Za-z0-9_-]+$`) in the JS handlers. [OBSERVED code path; not executed]
- NOTE: sovereign-control-server.js:163-169 `_recordOperation` retains up to 200 events x full `arguments` (<=1 MB each) per node in memory; `operationState()` copies them. Bounded by 200 x nodes; LOW.

- Rev-2 launch-source containment CONFIRM: conductor/launch-source.js:143-146 AUTHORITY_BOUNDARY_PROFILES keyed by boundary schema; :157-158 unknown => `typeof verify === "function" && ...` false => ticket malformed => unavailable. Legacy no-descriptor path :153 accepts only the voice-hook profile.
- R-14b CONFIRM: launch-source.js:204-206 the argv element after `--settings` is exempt from SHELL_METACHARACTERS (:62); its bytes are pinned only against the ticket's OWN `settings_sha256` (:111) i.e. producer self-attestation; the value legitimately contains `& <node.exe> <hook.js>` (:102-104, PowerShell call operator). Interaction with lead's R-14 (cmd.exe injection through quoted args): if `launch.executable` ever resolves to a `.cmd` shim, that `&`-bearing JSON goes through cmd.exe. [INFERRED; executable resolution is Python-side, not re-checked here]
- R-70 CONFIRM (mechanism): worker-spawn.js:297 scrubEnv(ticket, baseEnv) deletes ticket.launch.env_scrub_names (Python classification of process.env names; substring TOKEN in env-scrub.js:33 mirror) -> :298 augmentSpawnEnv re-adds SOVEREIGN_CONTROL_TOKEN (main.js:1949 / sovereign-control-server.js:173-178) -> :303-313 leftover guard finds SOVEREIGN_CONTROL_TOKEN in `names` ∩ env => refuse("failed") for EVERY worker spawn. Precondition: Electron's own process.env carries SOVEREIGN_CONTROL_TOKEN (e.g. shell launched from inside a governed child's environment) — narrow. Conductor path main.js:902-930 has no leftover guard (so it launches; not a leak since the re-added token is the intended one). Also SOW_VOICE_AUTH_TOKEN (turn-authority.js:118) is added by conductorVoiceAuthority.childEnv (main.js:917) and would trip the same class of guard if it existed there.
- R-73 CONFIRM: adapters/voice_parakeet/wsl_parakeet.py:189-194 confidence = 1.0 iff text non-empty (WSL script emits text only, :211-212); gate voice_bridge/conductor_voice.py:221 `source == VOICE and confidence < threshold`; command_broker.py:123 same. Disclosed at control_plane/orchestration/conductor_voice_feed.py:99 (U140).
- R-44 NARROW: main.js:1476-1480 clears residue on `/[\r\n\x03]/` (conductor-write.js:65-68) from pane:input. xterm.js auto-replies (DA `\x1b[?…c`, CPR `\x1b[r;cR`) contain none of those bytes, so a non-operator trigger needs a compromised renderer; effect = the next voice utterance may be spliced onto an unsubmitted body. LOW.
- R-45 CONFIRM/NARROW: main.js:498 paneSeq=0 per process; renderer.js:540-547 paneBadges.set(w.paneId, restored chrome) for snapshot panes; a NEW pane reusing `pane-N` (pane:new or a fresh worker before its chrome push) shows the stale badge — reconstruct rewrites node_state to `session_interrupted` (layout-reconstruct.js:106-116) so the label reads "interrupted (last run)"; overwritten by the next pane:chrome/spawn result. Cosmetic/observability, LOW.
- Recovery restore accepts from disk: layout-reconstruct.js:44-70 whitelists chrome fields; :224 `w.paneId` any truthy value; renderer uses paneId only as a Map key and esc()'d text. No file/IPC path built from it. OK.
- Voice non-operator path: preload.js:78 captureVoice(audioRef) with a STRING still reaches main.js:1644-1647 captureFromRef -> mock STT scripted refs (adapters/voice_parakeet/engine.py:103-111: "show status", "focus pane 3", "spawn worker"...) -> CHAT text typed+submitted into the LIVE conductor (deliverConductorChat) — reachable by any renderer script, not only the talk button (renderer.js:304-316 hardened the button only). Adds nothing beyond pane:input for text injection (renderer can already type), and it arms a non-executing voice turn; INFO.
- QUIT PATH: only unbounded await found = mainProcessLogger.flushAndDetach() (main-process-logger.js:204-209) awaits stdout write callbacks with no timer; precondition: a stdout pipe whose reader stalls without closing. [INFERRED]. Additional: gatewayProc.kill()/waitForChildExit measures py.exe (the launcher), not python.exe — see experiment below.

- R-56/R-57 CONFIRM: main-process-logger.js has no rotation (appendFileSync per line, :148); SOW_MAIN_LOG_FILE (main.js:313) used raw as the file path, unvalidated -> mkdirSync(dirname) (logger.js:134); every line mirrored to renderer over shell:log (main.js:315-317). Voice transcripts in .approvals/session-events.jsonl: begin() truncates once per PROCESS (main.js:2470); session-events.js:39-40 self-discloses "no TTL and no disposal at quit" (owed U208). Renderer receives every log line and console.log's it (renderer.js:575) — a log line embeds argv (main.js:976: full `claude --model ... --settings <profile-json>`), so the settings JSON (with hook command paths) is mirrored to the renderer DevTools console. INFO/LOW (loopback, local).
- turn-authority.js:684-728 stop() is bounded (DEFAULT_STOP_TIMEOUT_MS, budget/2 destroy) — U435; no unbounded await. childEnv:622-628 adds SOW_VOICE_AUTH_TOKEN.

### C. Executed
- `node pykill2.js` (scratch): `py -3.12 -c "...sleep(40)"`, printed python child PID, `p.kill()` the LAUNCHER. Result: launcher exit SIGTERM, and tasklist showed the python child GONE ("No tasks... 34820 not found"). So on THIS host `py.exe` forwards termination to python.exe — the `gatewayProc.kill()` in teardown (main.js:2520) does reap the real gateway. (py.exe is a thin launcher; the concern that killing it orphans python is NOT reproduced here.)
- Copied repo to scratch (robocopy, junctioned node_modules). `node --test test/*.test.js`: tests 1020 / pass 1020 / fail 0 (matches lead's desktop baseline; the one transient ERR_ASSERTION on the first run did not reproduce). 
- All 5 mutation harnesses on the scratch copy: pane_input_bypass ALL CAUGHT (23) byte-identical; disarm_authority, system_pane_write (32), readiness_signal (26), orchestration (14) — ALL CAUGHT, every file restored BYTE-IDENTICAL, EXIT=0. Harnesses are headless (node --test only; no Electron/provider). No SETUP-FAIL.

### D. Genuinely strong here
- The system→pane write gate (pane-writer.js) is a real bounded-screen classifier with echo-exclusion, gated on BOTH the body and every submit key, and its own comments honestly enumerate the residual gaps (U363 positive-evidence, U395 byte/geometry bound, U372 emulator). Reads the SAME injected window instance readiness uses (no drift).
- Voice submit gate: RingBuffer.sliceFrom() fails closed to null on a moved origin; deliverChat requires paneAcceptsTypedText + observed echo before the `\r`; the operator's ordinary keystroke (OS input path, before-input-event) is the only turn-end. Well-reasoned against the "CLI elicits an operator byte" bypass (main.js:1469-1475).
- SessionManager process-identity discipline (pid+generation) throughout; write/resize return HANDLE-level truth so ended sessions cannot report delivery; forget() refuses a live session.
- Mutation harnesses genuinely catch guard removal across main.js, pane-writer, worker-readiness, conductor-readiness, modal-affordance and restore byte-identically — the guards are falsified, not just asserted.
- childEnv scrub (env-scrub.js) mirrors the Python credential classifier and is pinned by a cross-language test; worker-spawn re-checks case-insensitively for leftover names and ABORTS the spawn (§2.2).

### E. Not covered / open
- Python side of R-46/R-70 (whether emit_worker_launch re-verifies host enumeration vs the renderer option; whether Python re-adds/classifies SOVEREIGN_CONTROL_TOKEN) — out of my area; the JS worker path re-checks host flags only for the CONDUCTOR select path, not the worker spawnFromSelection path (refuseSelection trusts sel.option).
- N-3 (control-char injection into system→pane bodies) is not exercised by any test I found; the U328 harness tests the SCREEN classifier, not body sanitization. Worth a Python-side check of whether collaboration_service validates proposition/objective/body for control chars before they reach notify_*.
- Did not run Electron/self-checks (prohibited); GUI-bound claims (permission handler, CSP enforcement) verified by code only.
