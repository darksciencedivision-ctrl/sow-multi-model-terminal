# REPORT_evidence — SOW independent review, area: evidence (tests/, tools/, docs/, root reports)
Started 2026-08-16. Incremental; sections appended as found.

## A. Adjudications (running)

### R-33 tools/mutation/_op12_close_mutations.py — CONFIRM (narrowed on the node path)
[OBSERVED] `tools/mutation/_op12_close_mutations.py:80` hardcodes `D:\Program Files\nodejs\node.exe`. That path EXISTS on this host (`ls "/d/Program Files/nodejs/node.exe"` ok), so it is portable-to-nowhere-else but not broken here. Every sibling harness (`_op18c_close_mutations.py:162`, `_op18d_amendment_mutations.py:131`, `_op18d_close_mutations.py:284`) uses bare `"node"` — op12 is the sole outlier.
[OBSERVED] Anchor 6 of 8 (`b'    if op12_probes is None and provider not in _OP12_PROVIDERS:\n        op12_probes = no_op12_probes()'`, harness :60-61) is NOT in the live `tools/live/emit_worker_launch.py`; the live code is `:306 if op12_probes is None:` / `:313 op12_probes = (no_op12_probes() if provider not in _OP12_PROVIDERS ...` — restructured by commit 93dcba0 (18C). Harness last touched at 03f1c13 (18B). So `main()` prints `[SETUP-FAIL]` for that entry, `bad += 1`, and returns exit 1 REGARDLESS of the other seven — the "8 RED / 0 GREEN, re-runnable" claim in `docs/evidence/PHASE18B_EVIDENCE_REPORT.md:143,153` and DECISION_REGISTER.md:167 is no longer reproducible from HEAD. I did NOT run the harness (it rewrites product files in place, `:101/:105`); I replicated the check by importing each `tools/mutation/_op*.py` module and testing anchor membership (script in scratchpad/evidence/anchors2.py; `git status` unchanged before/after).
Result across ALL 12 python mutation harnesses (anchors present/stale): op12 7/1 (STALE), op13 14/0, op18c_close 17/0, op18c_probe_path 15/0, op18d_amendment 8/0, op18d_close 24+5 JS/0, op18e_electron_wiring 22/0, op18e_hardening 10/0, op18e_live_acceptance 34/0, op18e_live_shape 11/0, op19_5_write_path 20/0, op19_policy_delegation 37/0. → the stale-anchor problem is CONFINED to op12; the later harnesses were maintained.
Also [OBSERVED]: op12 has none of the SIGINT/`_PINNED`/lock protections the 18E+ harnesses have (`_op18e_hardening_mutations.py:150-166`); a Ctrl-C between `:101` write and `:105` restore leaves the product file mutated. Its own comment in `_op18e_hardening_mutations.py:153` admits `_op18c_probe_path_mutations.py` also lacks it (U292(b)); op12 is not named there.
Fix shape: replace anchor with the :306/:313 form; use `shutil.which("node")`; add pin/lock like op18e.


### R-40 tools/run_phase19_pytest.py SIGTERM-only on POSIX — CONFIRM (+ unbounded post-kill wait)
[OBSERVED] `tools/run_phase19_pytest.py:38-39`: POSIX branch is `os.killpg(process.pid, signal.SIGTERM)` with no SIGKILL escalation; `:59 process.wait()` after `_terminate_tree` has NO timeout, so a pytest child that traps SIGTERM (or a grandchild that ignores it) hangs the runner forever after the ceiling — the exit-124 line at :60-62 is never reached. On Windows `taskkill /T /F` (:36) is fine. Precondition: POSIX host + a hung test; Windows unaffected. Severity LOW (this is a Windows-native repo; the runner is what `PHASE19_UNIT10_U339_SUITE_AND_GATE.md:99,126,156,160` cites as the executable contract).
Also [OBSERVED]: `:52 command = [sys.executable, ...]` — invoked as `python tools/run_phase19_pytest.py` on this host it would run the suite under 3.14 (which the tree's own docs say cannot collect); no interpreter pin/check. INFO.

### R-41 tools/evaluation/u326_before_after.py — CONFIRM (tool-only; not test-reachable)
[OBSERVED] `:33 sys.path.insert(0, str(pathlib.Path.cwd()))` (cwd-relative), `:39-40 subprocess.run(["git","show",...], check=True)` and `:46 exec_module(before)` all execute at IMPORT time; `:44 tempfile.mkdtemp()` never cleaned. Nothing under tests/ imports it (grep: only docs cite it), so the blast radius is "the reproduction command must be run from repo root with git present" — as its own docstring :14 states. NARROW severity to INFO/LOW; the doc-cited figure "19 of 34 scenarios differ" (PHASE19_UNIT2...CHECKPOINT.md:188, UNIT5:129) is reproducible only under those preconditions.

### R-31 root cause — which writers produced the 40 worktree-CRLF files (35 CRLF + 5 mixed; `git ls-files --eol`)
Config: `core.autocrlf=false`, `.gitattributes: * text=auto eol=lf` → git normalises on add (hence 0 CRLF blobs) but nothing re-normalises the worktree, so every CRLF file is a fingerprint of the writer.
Writer class 1 [OBSERVED code+file]: Python `Path.write_text(..., encoding="utf-8")` WITHOUT `newline="\n"` (Windows text mode → `\r\n`):
  - `tools/soak/phase2_soak.py:225` → all 5 `tools/soak/results/PHASE2_SOAK_*.json`
  - `tools/benchmark/phase6_probe.py:59` → `docs/evidence/PHASE6_ROSTER_PROBE.json`
  - `tools/providers/frontier_provider_recon.py:1454` → `docs/evidence/live/phase18a_host_recon.json`, `docs/evidence/receipts/PHASE18C_PROBE_ENGINE_REPORT.json`
  - `tools/live/real_parakeet_smoke.py:117` → `docs/evidence/receipts/PHASE16E_REAL_PARAKEET_SMOKE.json`
  - `tools/live/capture_claude_json_shape.py:63` → `tools/live/claude_json_shape.captured.json`
  Contrast: `tools/evaluation/u326_before_after.py:34` DOES pass `newline="\n"` — the fix is known in-tree but not applied to the emitters.
Writer class 2 [INFERRED]: Python `json.dump(..., sys.stdout)` (`tools/live/enumerate_pane_picker.py:466,476,482`) with shell redirection — CPython's redirected stdout on Windows translates `\n`→`\r\n` → `docs/evidence/live/phase15e_picker_enumeration.json` (2364 CRLF/0 LF), `phase16b_picker_enumeration.json`, and likely `phase18e_probe_*.json`, `PHASE18A_RUNNER_STATUS*.txt` (runner stdout captured).
Writer class 3 [OBSERVED]: `tools/loop/run_loop.ps1:60` `Set-Content $promptFile -Encoding UTF8` → `tools/loop/driver_prompt.txt` = BOM + exactly 1 CRLF + 25 LF (the here-string body carries the .ps1's LF; Set-Content appends one CRLF; PS5.1 UTF8 = BOM). `run_loop.ps1:67,:98` `ConvertTo-Json | Set-Content -Encoding UTF8` → `docs/loop/LOOP_STATE.json` CRLF body (162 CRLF) with LF islands at lines 3-4 and 163-171 = the fields the builder edited afterwards. Note the lead's BOM blob `harness.py` (BOM + 193 CRLF, last commit bc6b145 2026-07-18) has the same PowerShell `Set-Content`/`Out-File -Encoding utf8` signature — a builder-side PowerShell write, not a repo tool.
Writer class 4 [INFERRED, cannot pin to a site]: 12 all-CRLF `.py` sources + 2 `.md` (e.g. `adapters/conductor/adapter.py` 181/0, `control_plane/orchestration/live_debate.py` 855/0, `tests/integration/test_live_flow.py` 718/0) — mtime == last-commit day, no repo tool writes them; `adapters/frontier/claude_code.py` shows the tell-tale (810 CRLF with LF islands at exactly the later-edited hunks: 41-44, 75, 83-86, ...) — the builder's whole-file writer emitted CRLF and its subsequent hunk edits emitted LF. Print-mode iteration logs (`docs/loop/logs/*.log`, 354 present, gitignored) contain only assistant text, so the specific tool cannot be identified from them.
Fix shape: add `newline="\n"` at the 5 Python sites; write receipts with `open(..., "w", newline="\n")`; in run_loop.ps1 use `[IO.File]::WriteAllText(path, text, UTF8Encoding($false))`; consider a `git add --renormalize`-style worktree check in the freeze script.

### R-49 PACKAGE_MANIFEST "OPEN FINDINGS: 4" vs register — CONFIRM (the register cannot yield "4" under any parse); count method below
[OBSERVED] `PACKAGE_MANIFEST.txt` is not in the tree (only in the ZIP; lead N-07). Register `docs/registers/UNRESOLVED_ISSUE_REGISTER.md` = 9,583 lines, **444 distinct U-ids (U1..U444, contiguous, no gaps)**. It uses at least FIVE entry formats, appended over time and never rewritten: (1) the seed table `| U1 | ... | OPEN |` (12 rows, :8-19); (2) `**U13 (new, date, phase):**` bold paragraphs (:50-235, ~55 lines); (3) `## Register update <date> (phase) — U75/U76 RESOLVED, U79 opened` section headers (:1267-2837, 14 sections) whose bodies re-state states; (4) `### U300 — **OPEN: title**` / `### U327 - **CLOSED at ...**` headers (387 lines; 62 ids have ≥2 such headers, i.e. a second header re-files the id with a new state); (5) inline `**[[U329]] — CLOSED IN PART.**` bold-with-wikilink notes inside OTHER entries (e.g. :7561, :7678, :8160) and `State: **...**` trailer lines (≈40). Vocabulary of state tokens found: OPEN, CLOSED, RESOLVED, DISCHARGED, NARROWED, CARRIED, RECORDED, RESTATED, AMENDMENT, CORRECTION, CLOSED-BY-CORRECTION, CLOSED IN PART, FULLY CLOSED, FIRST PASS DONE, STATE CHANGE, DISPOSITIONS, MIXED, MIXED GATE DISPOSITION, "OPEN non-blocking", "OPEN mitigated", "OPEN BLOCKING", "IMPLEMENTED, RUNTIME MEASUREMENT OWED".
Counts (script `scratchpad/evidence/register_count.txt`): Method A — last opener/State line per id carries a closing token: 102 closed-ish / 238 open-ish / 104 unclassifiable → ~342 not-closed. Method B (most generous) — id mentioned ANYWHERE within 90 chars before CLOSED/RESOLVED/DISCHARGED/"is closed"/"closed with|by|at|on": 130 ids ever-closed-mentioned, **314 never**. Method B over-counts closures (it credits "CLOSED IN PART" and cross-references). So the honest range for "open" is ~314–342, versus the prior reviewers' ~321. "OPEN FINDINGS: 4" is not derivable from this file by any reading; it can only be a count of something else (e.g. blocking findings of one review). Never-closed seed rows: U1, U2, U4, U5, U7, U8, U9, U10, U12 (of U1–U12).
[NEW, OBSERVED] **Register/evidence divergence inside Phase 19's own charter range**: the 19.x evidence reports declare closures that the register never records: `PHASE19_UNIT5_U330_U332_ONE_WRITE_PATH.md:11` "U330 closed · U332 closed" — but the ONLY register lines for U330/U332 are the original `### U330 - **OPEN` (:6354) / `### U332 - **OPEN` (:6397) headers; commits 916056e/80e2e69 (19.5) added U410–U417 and repair notes but no U330/U332 closure line (`git show 80e2e69 -- register | grep U330` → only a C-3 cross-ref). Same for **U338**: commit 2359428 is titled "close U338 with executable coverage" yet its register hunk (U442) closes U435/U436/U439 and never states U338's disposition; register still `### U338 - **OPEN` (:6496) with no later line. **U337**: `PHASE19_UNIT8_U337_EVIDENCE_PROVENANCE.md:230` "U337 is closed" — register records it only indirectly via `### U441 - CLOSED: U337 and U437(e) now distinguish...` (:9486). **U339**: uncommitted diff adds U443 (CLOSED) and U444 (MIXED) but no U339 closure while `PHASE19_UNIT10_U339_SUITE_AND_GATE.md` is the closing doc. Directive §18 exit sentence (register :7064, :8574) is "every one of U326–U339 either closed with evidence or re-recorded with a reason" — by the register's own text U330, U332, U338, U339 are neither. Severity MEDIUM (governance/evidence integrity, not runtime).
Fix shape: a machine-readable disposition line per id (`State:` trailer) plus a script that fails the freeze if an evidence report claims a closure the register lacks.

### R-50 PHASE0_FREEZE_MANIFEST.json + DECISION_REGISTER head table PENDING vs closed; U7 OPEN — CONFIRM (by design of append-only, but the manifest is a frozen artifact so cannot be updated)
[OBSERVED] `docs/registers/DECISION_REGISTER.md:46-50` head table still shows D-UI-01, D-LANG-01, D-PERSIST-01, D-MCP-03, D-IPC-01 as `**PENDING-OPERATOR**`; the append-only rows at :63 (OP-3 conversion to operator-delegation), :66 (D-UI-01 CLOSED 2026-07-16), :68 (D-LANG-01 CLOSED 07-17), :72 (D-MCP-03 CLOSED 07-17), :73 (D-PERSIST-01 CLOSED 07-17), :86/:96 (D-IPC-01 DEFERRED then CLOSED 07-18) supersede them. `docs/PHASE0_FREEZE_MANIFEST.json:983-989 pending_operator_decisions` lists all five — and this file is inside the freeze-integrity hash (`operator_signature.instruction`: "Any later frozen-set change resets this to PENDING"), so it structurally CANNOT be updated without re-signing: stale by construction, which the register head table (:3 "Nothing here may be deleted; state changes append a dated note") shares. Register `U7 | Conflict mechanism confirmation (D-MCP-03) | P3A | OPEN` (:14) never received a closure note although D-MCP-03 CLOSED at :72 is exactly U7's question. NARROW: not a contradiction, but a reader-trap — anyone reading the head tables gets the wrong current state; the "current" view exists nowhere. Severity LOW; fix = a generated "current dispositions" view (script) rather than editing the frozen artifacts.

### R-52 PATH_A_PROVIDER_UNBLOCK_SHEET.md caveats stale — CONFIRM
[OBSERVED] `docs/operator/PATH_A_PROVIDER_UNBLOCK_SHEET.md` (last commit a3e6ccb 2026-08-06, the cold-audit commit; untouched since). `:32` "adapters/frontier/antigravity.py:72 currently set to accept-edits" — live `adapters/frontier/antigravity.py:78 ANTIGRAVITY_HEADLESS_MODE = "plan"` (restored at 19.1 per the comment :73-77; line 72 is now a comment). `:36-40` and `:62-64` "classifies from the whole retained pane scrollback with no recency window ... stays there permanently" — live `apps/desktop/control/worker-readiness.js:67,71,193,215` and `pane-writer.js:71-74` classify over `RingBuffer.sliceFrom(from)` windows (U363, 19.4 follow-on), and `main.js:1349` builds the slice. So both operator-facing caveats describe pre-19.1/19.4 code; an operator following STEP 0 (:12-15) would revert a line that is already reverted and misattribute AUTH_REQUIRED. This is the FIRST document a Path A operator is told to read, so it is stale at the point of maximum consequence. Fix: append a dated "superseded by 19.1/19.4" note or regenerate the sheet.

### R-53 emit_worker_launch.py usage text omits side effects — CONFIRM (narrow: docstring discloses lease; usage string and docstring both omit `operator_terms_confirmed=True` and node-record write)
[OBSERVED] Usage text `tools/live/emit_worker_launch.py:1131-1138` lists only argv shapes. The module docstring `:14-15` DOES say a frontier ticket takes "a durable I-X3 lease that is still held" and `:37-38` names `--release-session/--release-lease`; but neither docstring nor usage mentions that `main()` (a) hard-codes `operator_terms_confirmed=True` at `:1087` as a "citation of a recorded ruling" — i.e. the R8 §6 gate is satisfied by a constant at the production call site, exactly the pattern U292(a) removed from the library default (`:573-576`), just relocated one frame up; and (b) passes `registrar=default_provider_node_registrar(ROOT)` at `:1093` so an authorized ticket writes a node record into the operator's `.sovereign_store/` node log. Someone invoking the emitter by hand from the usage line gets a lease + a durable node record + a terms-confirmed gate with no flag to say so. INFO/LOW (tool-level; the shell is the intended caller). Fix: add a `--dry-run`/`--no-registrar` or at least three usage lines naming the three effects.

### R-75 tools/soak/results committed "and growing" — CONFIRM committed (1.5 MB, 10 files incl. two 671 KB `_events.jsonl`), REFUTE "growing"
[OBSERVED] `git ls-files tools/soak/results` = 10 files, all from ONE commit ca5aa71 (2026-07-17); nothing added since; `.gitignore` has no rule for it, so a future `phase2_soak.py` run (`:120-124,224-225` writes into the tracked dir) WOULD add files. Static, not growing. INFO. Same shape: `tools/spike_compositor/results/` (12 SPIKE_REPORT files, all 2026-07-16/17). Fix: ignore `tools/**/results/` and keep the canonical receipts under docs/evidence.

### R-76 spike_compositor divergent copies of ring-buffer.js/session-registry.js — CONFIRM but benign
[OBSERVED] `tools/spike_compositor/lib/ring-buffer.js` (15 lines) vs `terminal/session/ring-buffer.js` (84 lines, has `sliceFrom`); `session-registry.js` 27 vs 112. `tools/spike_compositor/package.json:5` says "Phase 1 throwaway spike rig ... NOT product code"; nothing outside `tools/spike_compositor/` requires it (grep for `spike_compositor` in *.js/*.py outside the dir → 0). The lead's BOM blob `tools/spike_compositor/test/manual_ws_probe.js` lives here. INFO: dead rig kept for the U9/U12 provenance; the risk is only that a reader mistakes it for the product ring buffer.

### R-77 no runner for terminal/test — CONFIRM
[OBSERVED] `terminal/` has 15 `test/*.test.js` files and NO `package.json` (`ls terminal/package.json` → missing); `apps/desktop/package.json:9` has `"test": "node --test test/*.test.js"` for its own dir only. The only recorded invocation is a bare shell line in `PHASE19_UNIT10_U339_SUITE_AND_GATE.md:153` (`node --test test/*.test.js  # terminal/`) and the op12 mutation harness (`_op12_close_mutations.py:80`, cwd `terminal`). No manifest, no `npm test`, no CI. LOW.

### R-79 tests/security only .gitkeep — CONFIRM
[OBSERVED] `tests/security/` contains only a 0-byte `.gitkeep` (2026-07-16). `tests/README.md:3-5` says it "receives the adversarial matrix (T1–T16)". No such matrix exists in tests/; the adversarial coverage that does exist is spread through `tests/unit/test_node_runtime_workspace_gate.py`, `test_live_authorization.py`, JS `apps/desktop/test/*`. The plan's promised structure was never populated; a reader trusting the README gets an empty directory. LOW (documentation/structure), but it means "security tests" cannot be pointed at.

### R-80 tests/evaluation asserts literal substrings — CONFIRM (narrow: it is a mock harness by its own admission)
[OBSERVED] `tests/evaluation/test_comparative_eval.py` (5 tests): `:49 assert "mock" in lim and "not model reasoning quality" in lim and "track e" in lim` pins the limitation prose; `:17` pins four config names; `:61` re-reads the COMMITTED `docs/evidence/PHASE13_EVAL_REPORT.json` and asserts `harness == "phase13-eval@1.0"` (a test that passes as long as the committed artifact keeps one field). Meaningful assertions: `:38-41` (debate costs more tokens than no-debate; C0 has 0 control ops). It tests the mock harness's bookkeeping, not evaluation quality — `tools/evaluation/harness.py` never touches a model. INFO.

### R-81 source-text structural pins — CONFIRM, quantify: 13 test files read product source text and assert on it
[OBSERVED] read_text/getsource over product files at: `tests/integration/test_frontier_process_tree.py:50` (process_tree.py), `tests/integration/test_mcp_server.py:180` (mcp_server/*.py), `tests/unit/test_conductor_selection.py:71` (selection.py), `test_emit_conductor_dispatch.py:288`, `test_frontier_provider_recon.py:1125`, `test_loop_runner_contract.py:13-21` (run_loop.ps1 + driver_prompt.txt — reads with `utf-8-sig`, i.e. it KNOWS these carry BOMs), `test_node_schema_amendment.py:268,522,595` (registry.py, compute_manifest.py), `test_op12_acceptance_constants_pinned.py:37` (verdict JS), `test_op12_frontier_adapters.py:344,740`, `test_op12_provider_scope.py:284`, `test_run_frontier_providers_ps1.py:38`, `test_session_approvals.py:135`, `test_statusbar_constants_pinned.py:42,100` (JS). Plus 5 `inspect.getsource` sites (test_emit_subscription_status, test_op12_probe_session, test_op12_provider_scope, test_worker_emitter_gate_inputs). These are "prose that cannot disagree with its value" tests — they go red on refactor, not on behaviour change; U338's own text (register :6496) condemns exactly this pattern for JS and 19.9 replaced some; the Python side still has 13 files of it. LOW/MEDIUM for maintenance.

### R-82 pytest.raises(Exception) x7 and mcp_client=object() — CONFIRM both, NARROW the second
[OBSERVED] `pytest.raises(Exception)` at test_conductor_succession.py:155, test_live_worker_legs.py:436, test_mcp_client_reconnect.py:158, test_opencode_harness_live.py:48, test_live_succession_zero_loss.py:256, test_worker_emitter_gate_inputs.py:175, test_worker_pane_spawn.py:332 — three of them (`:158`, `:175`, `:332`) capture `as exc/err` and assert on the message, which mitigates; four are bare. `mcp_client=object()` appears in ~45 constructor calls across 14 files (test_pane_node_spawn.py alone 20+). Production adapters DO call `self._mcp.call(...)` (adapters/conductor/adapter.py:74,127,142,157; adapters/local/worker.py:57,82; adapters/model_adapter.py:75,113; adapters/coding/opencode/driver.py:291), so those tests only ever exercise code paths that never reach the MCP client (build/spawn/refuse) — which is what they claim to test. NARROW: not wrong, but it documents that the MCP-integrated adapter paths are covered elsewhere (test_conductor_adapter/test_multi_model_adapters with fakes) — I did not find a test that composes a real MCP client with a spawn-path adapter.

### Skip discipline / hermeticity (task item)
[OBSERVED] 30 skip sites; gates found: `sys.platform != "win32"` x6, `os.name != "nt"` x2, PowerShell-present x2, `SOW_PROVIDER_HOST_CHECK` x1, Ollama-present x3, `_REASONING_LIVE` x1, `shutil.which("node")` x1. Hard-coded Windows literals in tests are strings passed as workspace params ("D:/repo", "D:/ws" — neither exists on this host, so they are pure data, not host coupling). Windows-only spawns without a gate: none found (my scan's hits were JSON keys named "node"/"wsl", or monkeypatched `shutil.which`). The one real hermeticity hole is the `tests/unit/test_op12_frontier_adapters.py:740` read of the repo's tracked `.grok/config.toml` and `test_provider_node_registration.py:319` read of `.gitignore` — repo-state, not host-state; fine. Env/PATH mutation is done via `monkeypatch` everywhere I looked; `tests/live_call_guard.py` is installed autouse (`tests/conftest.py:17-22`) and I did not find any test that opts out via `live_cli` (grep `live_cli` → only conftest/guard/README/pytest.ini): so the marker is a documented but unused escape hatch — good. Root `conftest.py:22-31` marks phase19 subsets by path with `Path.resolve().relative_to(ROOT)` — would raise ValueError if a test file were collected from outside ROOT (e.g. `--rootdir` tricks); INFO. Nothing in tests reads real `~/.claude`, `~/.codex`, `~/.grok`, or the real `.sovereign_store` (grep negative; `test_op12_provider_scope.py:388` comment explains the scratch-ledger override).

### C5 FINAL_LIVE_REPORT.md internal contradictions + receipt support — NARROW: receipts SUPPORT the narrative; the file is a stack of un-superseded addenda
[OBSERVED] `FINAL_LIVE_REPORT.md` (493 lines; last commit cd08878 2026-08-02) contains: §5 runbook `:79-82` "cite OP-6, scope {providers:[claude_code, codex], terminals_per_subscription: 2}"; §1/§2 (17E era, `:202-222`) "the conductor pane runs the real claude CLI and answers you"; 18D addendum `:280-340` and 18E addendum `:366-458` "OP-12 ... four providers ... grok-4.5 / gemini-3.6-flash-low". `grep -ci "errata\|erratum"` → 0; no "superseded" marker on §5. So the runbook an operator would follow first names OP-6/two providers although the tracked `config/live_operation.example.json` now ships `register_row: OP-12`, four providers, `terminals_per_subscription: 2` (with per-provider cap 1 for the OP-12 pair per `_comment_terminals`). This is the same "prose that cannot disagree with its value" failure the repo names for itself. MEDIUM (operator-facing).
Receipt spot-checks (task item): `docs/evidence/receipts/PHASE17E_FULLY_LIVE_SELFCHECK.json` — `checks` is a dict of exactly 36 keys, all `true`; `failed_checks: []`; `typed.answer_matched: true` (expected 87, `answer_number: 87`), `voice.answer_matched: true` (73/73, real transcript "Use a tool to calculate 30 plus 43..."), `concurrency: {allowance: 2, scope: "diagnostic", max_in_use_observed: 2, samples:[1,2,2,...]}`, `source.commit fa3e1a1`, `tracked_product_tree_clean: true`, `env.electron 31.7.7`. → "36/36", "answers you" (typed AND spoken), and "I-X3 = 2" ARE supported by the receipt's own fields; note the allowance-2 is labelled `scope: "diagnostic"` in the receipt while the report says it plainly. `PHASE18E_LIVE_ACCEPTANCE_SELFCHECK_phase-18e.close_20260802T082536Z.json` — `ok: false`, `providers_attempted: [grok_build, google_antigravity]`, `providers_live: []`, `providers_consent_gated: both`, `live_exchanges_spent: 0`, per-leg `skipped_with_record: true`, `answer_seen: false`, `submitted: false`, argv `grok.CMD ... -m grok-4.5` / `agy.EXE --mode plan --model gemini-3.6-flash-low`, `lease_released: true`, `node_attested/node_closure` READY→TERMINATED. → `PHASE18E_EVIDENCE_REPORT.md:34,194` says exactly this ("ZERO ... NOT RUN — consent gate"), and FINAL_LIVE_REPORT `:450-458` claims only spawn/lease/node-record for both providers, plus `:443-444` "one of which (Antigravity) answered" attributed to `.live.shape` (a different receipt, `docs/evidence/live/phase18e_probe_document_shape_*.json`). Consistent. What the receipts do NOT support is any reading of FINAL_LIVE_REPORT that both OP-12 providers answered a live prompt inside the packaged shell — they never did (0 exchanges on all four 18E close runs).

### C6 canonical Handoff.md:421 (I-X3 default 1, "configurable only up to the provider's actual per-account concurrency allowance") vs register U5 OPEN — CONFIRM the tension, NARROW: it is disclosed
[OBSERVED] `docs/canonical/Sovereign_Orchestration_Workspace_Canonical_Handoff.md:421` binds raising I-X3 to a verified provider allowance. Register `U5` (:12) is OPEN; its updates `:181-186` and `:203-207` say "**no verified per-account concurrency number for EITHER provider** (both empirically verified-at-1, fail-closed); the effective allowance 2 is operator-ordered (OP-6 §11(b)), governor-capped at 2, reversible — operator authority, NOT" a verification. Directive `AUTONOMOUS_BUILD_DIRECTIVE.md:183` "I-X3 allowance is raised to 2 terminals per subscription by explicit" operator instruction. So the canonical invariant's condition ("up to the provider's actual allowance") was replaced by operator fiat, and the register says so. The 17E receipt's `concurrency.scope: "diagnostic", allowance: 2` (see C5) is the run that exercised it. Not hidden; but the canonical text was never amended, so a canonical-first reader sees I-X3 violated. LOW.

### C7 AUTONOMOUS_BUILD_DIRECTIVE.md:583 "Only then does Path A begin" vs bring-up on a dirty tree — CONFIRM as a sequencing breach, with the mitigating record
[OBSERVED] `:578-585` requires U326–U339 closed-or-re-recorded, both reviewers PASS, all three suites reported, and a gate tag BEFORE Path A, "run against a committed HEAD". Live tree: `git status` shows `M docs/loop/LOOP_STATE.json`, `M docs/registers/UNRESOLVED_ISSUE_REGISTER.md`, untracked `PHASE19_UNIT10_U339_SUITE_AND_GATE.md`; no `gate/phase-19*` tag (`git tag` — see lead); the uncommitted LOOP_STATE `next_step` itself records "spec-auditor round 1 returned FAIL with SIX blockers and its report was NEVER PERSISTED ... gate-validator round 1 was INTERRUPTED ... produced NO VERDICT ... No tag until both reviews PASS". The lead's own report (INDEPENDENT_REVIEW...:114,202) refers to "the Codex conductor bring-up" — a Path-A-shaped live action — while the gate is open. I could not find a receipt for that bring-up in `docs/evidence/` (no 08-15 receipt files; latest receipts are 08-02), so the "08-15 bring-up" is [UNVERIFIED] from the tree; what IS observed is that Path A's precondition is unmet and the LOOP_STATE says so. MEDIUM (process), not runtime.

### C10 mutable_audit_files check treats truncation/rewrite/append alike — CONFIRM
[OBSERVED] `tools/manifest/compute_manifest.py:244-247` labels registers/evidence "Append-only ... NOT checked for drift"; `--check` at `:269-276` classifies ANY hash change as `info "mutable changed (expected, append-only)"` and a missing entry is not even reported (only `new` and `changed`; a REMOVED register file produces nothing because iteration is over the NEW manifest's entries). So a rewritten, truncated, or deleted register passes `--check` identically to an appended one. The append-only invariant the registers state at their heads (UNRESOLVED :4 "Entries are never deleted"; DECISION :3) has no executable guard. Fix shape: for mutable files, assert the old content is a byte-prefix of the new (`new.startswith(old)`), and report REMOVED.

### R-87 "OPEN_READY appears once repo-wide" — REFUTE on the live tree (0 occurrences)
[OBSERVED] `grep -rn "OPEN_READY"` (and case/underscore variants) over the tree excluding node_modules and gitignored logs → 0 hits in code, docs, registers, receipts. Whatever the prior reviewer saw was in the ZIP-only files (STATE_OF_THE_WORKSPACE.md / PACKAGE_MANIFEST.txt, lead N-07) or a log. Not a repo claim.

### R-88 stale cites AUTONOMOUS_BUILD_DIRECTIVE.md:554 (antigravity.py:72) and U437(a) main.js:2957 vs 2905 lines — CONFIRM stale NOW, NARROW: both were accurate when written
[OBSERVED] Directive `:549` cites `adapters/frontier/antigravity.py:72` returning to "plan"; live constant is at `:78` (`:72` is inside the restored comment block :69-77 — the OP-13 revert itself added the 6 comment lines that moved it). Register `:9405` cites `main.js:2957` (`was_listening` gate) — at the commit that wrote it (2ee1c5a, 19.7 close) `git show 2ee1c5a:apps/desktop/main.js` had 3264 lines with `was_listening` at :2957; after 19.9's extraction (324811b) main.js is 2905 lines and the site is `:2594`. Append-only registers + line-number citations guarantee this decay; the repo has no cite-checker. INFO. Fix: cite symbols/anchors, or a `tools/` script that validates `path:line` cites contain the quoted token.

### apps/desktop/selfcheck coverage / reader question (task item)
[OBSERVED] 36 files in `apps/desktop/selfcheck/`. Only **12** are `require`d by any `apps/desktop/test/*.test.js` (script: match `selfcheck/<stem>` in test sources): the pure "verdict"/reader modules (`fully-live-verdict.js`, `op12-acceptance-verdict.js` x3, `op12-live-acceptance-verdict.js`, `op18d-registration-verdict.js`, `process-tree.js`) plus `op12-acceptance-selfcheck`, `op12-live-acceptance-selfcheck`, `op18d-registration-selfcheck`, `orchestration-collaboration-selfcheck`, `readiness-window-selfcheck`, `system-pane-write-selfcheck`, `voice-conductor-selfcheck` (2). **24 have zero direct test coverage**, including the largest drivers: `fully-live-selfcheck.js` (999 lines — the 17E "36/36" producer), `worker-spawn-selfcheck.js` (691), `worker-launch-selfcheck.js` (566), `runtime-honesty-selfcheck.js` (422 — 19.8's provenance producer; `runtime-honesty-wiring.test.js:20` only greps `main.js` text for the wiring), `conductor-*` (7 files), `voice-*` (3 of 4), `pane-*`, `picker`, `recovery`, `statusbar`, `approvals`, `assembled`. `selfcheck-guards.test.js:30,63,69` does load EVERY selfcheck file (readFileSync + `require` doesNotThrow) — so they are syntax/require-checked but their check logic is exercised only inside Electron. Consequence: a receipt like PHASE17E's 36 booleans is produced by 999 lines that no unit test grades; the "verdict" halves that DO have tests grade the receipt's SHAPE (which keys must be true), not whether the producer measured what its key names say. This is exactly the U337 "testimony shaped like a receipt" class one layer down; 19.8/19.9 addressed it for orchestration/runtime-honesty wiring but the 17E/18E producers remain untested. MEDIUM (evidence-integrity), fix shape: split each producer's measurement functions into require-able pure modules and grade them with fakes (as 19.9 did for orchestration).
