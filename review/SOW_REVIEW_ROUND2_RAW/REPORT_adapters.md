# REPORT_adapters (incremental; HEAD bad029e7)

## Notes as I go (raw; consolidated at end)

### process_tree.py (read fully)
- R-60 CONFIRM [OBSERVED]: `_run_windows` (process_tree.py:169-266) never uses its `stdin` parameter; Popen at :194-202 hardcodes `stdin=subprocess.PIPE`, communicate writes "GO\n"+input_text (:219-220). Member (`_job_member_main` :140-166) reads exactly 4 bytes then hands its stdin pipe to the target (:159). Practical effect: target sees EOF after input (DEVNULL-like), so no hang; but `stdin=DEVNULL` requests (provider_cli_common.py:1202) are silently not honoured, and any input_text passes through a text-mode pipe (`text=True` :195) so "\n" -> "\r\n" newline translation reaches the target's stdin (member reads b"GO\r\n" :150 proves this).
- R-61/N-04 mechanism [OBSERVED]: after a CLEAN exit `finally` still calls `job.terminate_and_wait(15.0)` (:227). `terminate_and_wait` (:125-132) skips TerminateJobObject when pids() is empty but ALWAYS calls `WaitForSingleObject(job_handle, 15000)` (:128); a Job handle is only signalled on a job-time-limit expiry, so with no processes left this is an unconditional 15 s sleep. Every managed call pays it.
- R-62 CONFIRM [OBSERVED]: `_run_posix` (:269-318) `finally` always `os.killpg(proc.pid, SIGTERM)` (:290) even after clean exit; harmless when group already gone (ProcessLookupError caught), but if the CLI left a detached descendant in the pgid it gets SIGTERM'd -> that is a feature; no defect beyond the timing.
- R-38 CONFIRM [OBSERVED]: process_tree.py:195 (member Popen), :242 (taskkill), :273 (posix Popen) all `text=True` with no `encoding=`/`errors=`; :157-163 member->target passes inherited handles (bytes flow directly to the parent's pipe). Parent decodes with locale preferred encoding (cp1252 on this host unless PYTHONUTF8). NEED TO PROVE: what happens on an undefined cp1252 byte inside communicate() reader threads.

### F3 / R-25 EXECUTED (scratch script f3_probe.py; no CLI spawned; cli_present injected)
- A: no descriptor, EMPTY ledger, selection "opus-4.8" -> argv `['claude','--model','opus-4.8']`, model_probe.source="unprobed", model_available=None. Label carried verbatim to argv (claude_model_probe.py:307-309 -> conductor_pane_spawn.py:248-259 via resolve_claude_model_ref which never refuses, claude_code.py:118-119). No validation of the slug shape at all on the Claude path (only `.strip()`).
- B: no descriptor, HOST ledger -> `--model claude-fable-5` (probe-ledger). Ledger works when consulted.
- C2 (**NEW, HIGH**): the descriptor path that the shell's `--emit-conductor-launch` main ALWAYS takes (emit_conductor_launch.py:532-533 passes `descriptor=load_runtime_conductor_descriptor()`) sets `model = desc.model_id; model_available = True` at :270-271, so the ledger read at :293 is skipped (`model is None` false). Result with the real host ledger present: argv `['...\claude.EXE','--model','fable-5']`, model_probe = {model:'fable-5', model_available:True, source:'registered-exact-slug'}. The host ledger (.sovereign_store/model_probe/claude_code.json) records that this exact slug was REJECTED by the CLI on 2026-07-26 ("There's an issue with the selected model (fable-5)") and that `claude-fable-5` is the accepted one. So the 17A `.roundtrip` fix is regressed on the production path and the ticket asserts `model_available: True` without evidence. Precondition: conductor preference is Claude (config/live_operation.json currently prefers openai_codex_cli/gpt-5.6-sol, so latent on this host today; absent `conductor` block -> registry.py:176-186 defaults to claude/fable-5). Same for opus-4.8 (C3). Tests only cover the no-descriptor path (test_emit_conductor_launch.py:405-442).
- resolve_model_ref (provider_cli_common.py:1242-1261) is used only by grok_build.py:240 / antigravity.py:186 with `available` from the probe inventory; with empty inventory (`available` falsy) it carries the slug unverified. Claude/Codex conductor path uses resolve_claude_model_ref/resolve_codex_model_ref which never consult any inventory. R-25 CONFIRM.

### Executed pure checks (pure_checks.py; host preferred encoding = cp1252, PYTHONUTF8 unset)
- R-48: parse_version("npm WARN update 1.2.3 → 2.0.0") == (1,2,3) [OBSERVED]. Also "npm notice New major version of npm available! 10.9.2 -> 11.4.2" as first line -> (10,9,2). So the banner does NOT fail closed: probe_provider_cli (provider_cli_common.py:1019-1022) takes `lines[0]` of `(out or err)` and `_SEMVER_RE.search` returns the FIRST triple -> a wrong-but-valid version, `meets_minimum` True (min is 0.1.0). NARROW: "fails closed" part of the claim is wrong; it fails OPEN to a wrong version string. Low impact (min version 0.1.0; version is display only) but the recorded `version` field in evidence would be npm's.
- R-85 CONFIRM [OBSERVED]: `_flag_pairs` (provider_cli_common.py:274-287) emits a pair for EVERY token incl. values, so `["grok","-m","tools"|"agent"|"allow"|"worktree"|"yolo"|"-Tools"]` all raise ValueError from assert_no_forbidden_provider_args (:312-316). Refusal-only (fail closed), no bypass; but a legitimately named model can never be selected. `-m x=agents` passes (split on '=' -> name 'x'). Path values like `--cwd D:\proj\--tools` pass (whole token compared).
- R-84 CONFIRM [OBSERVED]: classify_provider_outcome(None, "hello", "") -> FAILED / basis "nonzero-exit" (:449 `exit_code == 0` false for None; :460+). Reachable only from a runner that returns rc=None without timed_out/spawn_error; run_managed_process always returns an int returncode; recon subprocess_runner to be checked below.
- R-24 CONFIRM [OBSERVED]: classify(1, stdout="Error: not logged in", stderr="warning: config.toml deprecated key") -> outcome AUTH_REQUIRED but detail == stderr text; `is_auth_pause_text(outcome.detail)` == False. So FrontierProviderCliBackend.generate (:1211-1219) would raise RuntimeError (task failure) instead of ProviderAuthPause (pause) whenever the auth marker sits on the non-preferred stream, or beyond the 600-char truncation (also demonstrated: 650 x's + " please log in" -> False). Latent today: generate refuses at :1180 while _LIVE_SPAWN_PATH_WIRED is False for both OP-12 backends (grok_build.py / antigravity.py set False). Fix shape: branch on `outcome.outcome in (OUTCOME_AUTH_REQUIRED, OUTCOME_USAGE_LIMIT)`.
- Scrub matrix [OBSERVED] (cl=claude, cx=codex, op=OP-12; conductor path provider_commands.py:84-91 = cl|cx; worker path worker_pane_spawn.py:227-256 = cl|cx|op):
  NODE_OPTIONS / NODE_EXTRA_CA_CERTS: op only -> SURVIVE on conductor path (Claude AND Codex conductor panes) — R-42 CONFIRM. NODE_PATH survives everywhere — R-43 CONFIRM. Also surviving on ALL paths: SSL_CERT_FILE, REQUESTS_CA_BUNDLE, CURL_CA_BUNDLE, NPM_CONFIG_REGISTRY, PIP_INDEX_URL, GITHUB_PAT, MY_PASSWD, DB_PASS, SESSION_COOKIE, CLAUDE_CONFIG_DIR, CODEX_HOME, GROK_SANDBOX, ELECTRON_RUN_AS_NODE, PYTHONSTARTUP, BASH_ENV, LD_PRELOAD (irrelevant on Windows), HTTPS_PROXY (by design U264).
  Claude-only classifier misses (cl=0) but codex/op catch: AZURE_OPENAI_KEY, STRIPE_KEY, OPENAI_KEY, NPM_CONFIG__AUTH, DOCKER_AUTH_CONFIG, NODE_TLS_REJECT_UNAUTHORIZED (caught by cx/op via "AUTH" substring, incidental). Because conductor/worker paths union, only a Claude-only consumer would miss these — ClaudeCliBackend.build_env (claude_code.py:414-418) IS such a consumer (headless worker + probe_claude_model), and it also leaks NODE_OPTIONS/NODE_EXTRA_CA_CERTS.
  Over-scrub (false positives) on cx/op via bare "KEY"/"AUTH": KEYCHAIN, MONKEY_PATCH, TURKEY, AUTHOR — harmless but shows the net is substring-based, so `*_KEY_PATH`-style non-secrets are also dropped.

### R-38 EXECUTED (enc_probe.py; python child emitting UTF-8 JSON containing U+276F "❯" = E2 9D AF and U+2192 "→" = E2 86 92; host preferred encoding cp1252, PYTHONUTF8 unset; nothing in the repo sets PYTHONUTF8 — only run_frontier_providers.ps1:231-250 sets PYTHONIOENCODING, which affects the *engine's* stdio, not subprocess pipes)
- A  `subprocess.run(..., text=True)` (codex.py:219 CodexCli._run and codex.py:658 CodexCliBackend.generate): byte 0x9D -> the stdlib reader thread dies with UnicodeDecodeError printed to the PARENT's stderr, `proc.stdout` is **None** and rc stays 0. Consequences by code read: CodexCli.version() -> `out=""` -> CodexUnavailable "`codex --version` exited 0 with output ''" (:229-232) i.e. present CLI reported ABSENT; CodexCliBackend.generate -> `out=""` -> returns json.dumps({"codex_exec":"empty","stderr":...}) (:671) as a SUCCESSFUL result -> published as CANDIDATE.
- A2 defined-but-non-ASCII bytes ("→"): decodes as mojibake `'â†’ done'`; JSON parses; text corrupted. Every "—", "→", "✓", curly quote in a Claude/Codex reply is corrupted on these paths.
- B  `run_managed_process` (process_tree.py:195 member Popen text=True; used by claude_code.py:460, provider_cli_common.py:1201, provider_probe_session.py:529): 0x9D -> stdout '' rc 0 (reader thread death is swallowed by `stdout[0] if stdout else None`), **elapsed 15.2 s** (N-04 tax reproduced). Consequences: ClaudeCliBackend.generate -> json.loads("") -> RuntimeError "non-JSON output" (loud, but the call was SPENT and the reply lost); FrontierProviderCliBackend.generate -> extract_provider_text("") == "" returned as SUCCESS; supervised probe -> exit 0 + empty transcript -> refused with a misleading reason (`describe_provider_document` note "no output was read").
- Inventory of `text=True` w/o encoding in scope: process_tree.py:162,:195,:242,:274; codex.py:219,:658; adapters/coding/opencode/{candidate.py:69, driver.py:107,:199,:362, harness.py:283}. WITH encoding: frontier_provider_recon.py:709-712 and :1410-1412 (utf-8, errors=replace). Which real bytes trip it: any UTF-8 sequence containing 0x81/0x8D/0x8F/0x90/0x9D as a continuation byte — e.g. U+276F ❯ (E2 9D AF, common prompt glyph), U+2590 ▐, U+258F ▏, U+258D ▍, U+2581 ▁ (block/progress glyphs), emoji in U+1F340-1F37F/1F3C0-1F3FF/1F400-1F43F (F0 9F 8D/8F/90 xx: 🍎🏆🐍…). R-38 CONFIRM and widened: it is not just mojibake — undefined bytes silently BLANK the whole stream.
- Fix shape: `encoding="utf-8", errors="replace"` (or "surrogateescape") on every Popen/run in scope, matching recon.py:712.

### N-04 count (code read only)
- `run_frontier_providers.ps1 -Action status` -> Invoke-StatusAction (:315-317) -> ONE python engine process `--action status` -> build_report (recon.py:1034-1045) -> probe_status per spec, which uses `subprocess_runner` (bare subprocess.run, recon.py:707-717) — **0 managed calls**, so 0 s of the 15 s tax; ~2 bare calls per provider (`--version`, `models`). The runner covers OP-12 only (`--provider all|grok|gemini`, recon.py:1433; ps1 ValidateSet) = 2 providers, not 4. Only `-Action probe` uses run_managed_process (provider_probe_session.py:529 via _supervised_runner) -> 1 managed call per selected provider -> 2 x 15 s = ~30 s tax for `-Provider all`, plus the Windows member hop. Install/login actions issue another status engine run each (:332, :421) — bare calls only.

### NEW — prompt/objective text reaches cmd.exe via the `.cmd` shim (EXECUTED cmd_inj2.py with a scratch fakecodex.cmd that only echoes %*)
Host resolution (shutil.which, no execution): codex -> C:\Users\Sslaw\AppData\Roaming\npm\codex.CMD; grok -> ...\npm\grok.CMD; opencode -> ...\npm\opencode.CMD; claude -> claude.EXE; agy -> agy.EXE.
CodexCliBackend.build_command (codex.py:605-622) appends the PROMPT as the last argv element; generate (:658) runs it with subprocess.run(list). Through the .cmd shim (cmd.exe /c), with list2cmdline quoting:
  - prompt 'Summarise Q3 report" & echo INJECTED2 & rem "' -> stdout shows `INJECTED2` executed as a SEPARATE command (rc 0).
  - prompt 'x %PATH:~0,5% y' -> argument became 'x C:\Us y': cmd.exe EXPANDS %VAR% inside the prompt, so any env var of the (scrubbed) child env can be pulled INTO the text sent to the provider.
  - prompt 'a\nb & echo INJECTED3' -> argument became 'a' only: the prompt is silently TRUNCATED at the first newline (multi-line objectives lose everything after line 1).
  - plain '... & echo X' inside a single quoted arg did NOT execute (quotes intact) — the break-out needs a `"` in the prompt, which any objective quoting text has.
The prompt is `f"Task {task_id}. {objective}"` where objective is MCP context content (adapters/model_adapter.py:76-84) — node-controlled T2 input. Same shape for OpenCode (`opencode.CMD`, prompt positional in adapters/coding/opencode/harness.py:141-149, run via driver.py:107) and for grok.CMD (`-p <prompt>`, grok_build.py; latent — generate refuses). This widens the lead's R-14 (paths/slugs) to PROMPT CONTENT and adds two non-security effects (env expansion into prompt; newline truncation). Precondition: the live Codex worker path (codex_spawn.attempt_codex_live_smoke is wired) with `codex` resolved to the npm .CMD shim (this host). Claude (claude.EXE) and agy (agy.EXE) are NOT affected. Fix shape: resolve the shim to `node.exe <script.js>` and spawn that, or refuse .cmd/.bat executables for any argv carrying free text.

### More executed predicate checks (pred_checks.py)
- parse_grok_models("Tip: if you are logged in elsewhere, run `grok login` again.\n\nAvailable models:\n  * grok-4.5 (default)") -> login_reported=True (the `_GROK_LOGIN_MARKER_RE` is a `search` on every line, provider_cli_common.py:821,:886) -> AUTH_AUTHENTICATED with exit 0. Identity in the login line ("sam@example.com") is correctly withheld (account_hint None).
- parse_antigravity_models("Unauthorized\n401\nError:\nrefreshing...\nplease-login\ngemini-3-pro") -> models ['Unauthorized','401','refreshing...','please-login','gemini-3-pro'], parse_note "ok" (:926-941: any space-free line matching MODEL_SLUG_RE is a model). pane_picker.py:353-370 turns each into an option with verified=True.
- CodexCliBackend zero-exit stdout demotion (codex.py:667-670): "The API enforces a per-user quota of 100 req/min…" -> CodexAuthError PAUSE on a SUCCESSFUL exit; "Handle HTTP 429 (rate limit) by exponential backoff." -> PAUSE. Only JSON-looking output ("{") is exempt; `codex exec` prints plain text. Violates the §6 exit-code-first rule this repo states elsewhere.

======================================================================
# FINAL REPORT — area: adapters (HEAD bad029e7, Windows host, cp1252 locale)
======================================================================

### A. Adjudication of prior claims
| Prior ID | Verdict | Evidence (path:line) | Notes |
|---|---|---|---|
| R-08 recon stdout-only grok parse, ignores auth_required_reported | CONFIRM [OBSERVED] | tools/providers/frontier_provider_recon.py:946-947 `parse_grok_models(out)`; :951-965 reads only `inv.login_reported`; sibling adapters/frontier/provider_cli_common.py:1035-1039 feeds `f"{out}\n{err}"` and :1044-1046 honours `auth_required_reported` | Executed: stdout-only parse of a signed-out transcript gives login_reported=False / auth_required_reported=False, so recon reports AVAILABLE + "UNVERIFIED session" caveat while `probe_provider_cli` reports AUTH_REQUIRED for the same CLI output. If the line is on stderr (as the sibling's own comment asserts) recon can never report AUTHENTICATED or AUTH_REQUIRED for grok. |
| R-24 generate re-derives pause from outcome.detail | CONFIRM [OBSERVED] | provider_cli_common.py:1211-1219; detail built at :463 `redact_diagnostics(stderr or stdout)` (600-char cap :360) | Executed: classify(1, stdout="Error: not logged in", stderr="warning: ...") -> outcome AUTH_REQUIRED but `is_auth_pause_text(detail)`==False -> RuntimeError (task failure) not ProviderAuthPause; a marker past 600 chars is also lost. Latent: generate refuses at :1180 (`_LIVE_SPAWN_PATH_WIRED` False in grok_build.py / antigravity.py). |
| R-25 (F3) no argv builder validates --model; resolve_model_ref refuses only when `available` truthy | CONFIRM + WIDENED [OBSERVED] | provider_cli_common.py:1250-1261; claude_code.py:110-120, 398-401; conductor_pane_spawn.py:248-259; tools/live/emit_conductor_launch.py:262-271, 293-300 | Executed with EMPTY ledger: explicit "opus-4.8" -> argv `['claude','--model','opus-4.8']`, source "unprobed". Executed with the real host ledger AND the descriptor the shell's `--emit-conductor-launch` main always passes (:532-533): argv `--model fable-5`, model_probe {model_available: True, source: "registered-exact-slug"} — the ledger (fable-5 REJECTED, claude-fable-5 accepted) is BYPASSED. See B-1. |
| R-38 process_tree text=True no encoding | CONFIRM + WIDENED [OBSERVED] | process_tree.py:162, 195, 242, 274; codex.py:219, 658; opencode candidate.py:69, driver.py:107, 199, 362, harness.py:283; WITH encoding: recon.py:709-712, 1410-1412 | Executed: child emitting UTF-8 containing 0x9D (U+276F) -> subprocess.run(text=True): reader thread dies, `stdout=None`, rc 0; run_managed_process: stdout '' rc 0 (15.2 s elapsed). Defined non-ASCII bytes ("→") decode as mojibake `â†’`. See B-2. |
| R-42 conductor scrub unions only Claude+Codex; NODE_OPTIONS / NODE_EXTRA_CA_CERTS only in OP-12 classifier | CONFIRM [OBSERVED] | adapters/conductor/provider_commands.py:84-91; provider_cli_common.py:75; worker_pane_spawn.py:227-256 unions all three | Matrix executed: NODE_OPTIONS / NODE_EXTRA_CA_CERTS: claude 0 / codex 0 / op12 1 -> survive on the conductor path (Claude AND Codex conductor panes) and on ClaudeCliBackend.build_env (claude_code.py:414-418: headless worker + model probe). Scrubbed on the worker-pane path. |
| R-43 NODE_PATH survives all scrubs | CONFIRM [OBSERVED] | same matrix | Also surviving everywhere: SSL_CERT_FILE, REQUESTS_CA_BUNDLE, CURL_CA_BUNDLE, NPM_CONFIG_REGISTRY, PIP_INDEX_URL, GITHUB_PAT, MY_PASSWD, DB_PASS, SESSION_COOKIE, ELECTRON_RUN_AS_NODE, PYTHONSTARTUP, BASH_ENV, CLAUDE_CONFIG_DIR, CODEX_HOME, GROK_SANDBOX (by design). Claude-only classifier additionally misses AZURE_OPENAI_KEY, STRIPE_KEY, OPENAI_KEY, NPM_CONFIG__AUTH, DOCKER_AUTH_CONFIG (no bare KEY/AUTH/CREDENTIAL substrings, claude_code.py:166) — caught by codex/op12 wherever the union applies. |
| R-48 version first line only; npm banner fails closed; "npm WARN update 1.2.3 → 2.0.0" -> (1,2,3) | NARROW [OBSERVED] | provider_cli_common.py:807-814, 1019-1022; recon.py:927-930 | (1,2,3) confirmed. But an npm banner does NOT fail closed: "npm notice New major version of npm available! 10.9.2 -> 11.4.2" as line 0 parses (10,9,2) -> meets_minimum True (min 0.1.0) with npm's version recorded as the CLI's. Fails OPEN to a wrong version string; low impact. |
| R-60 _run_windows ignores stdin arg | CONFIRM [OBSERVED] | process_tree.py:169-172 (param unused), 194-202 `stdin=subprocess.PIPE` hardcoded, 219-220 | Target sees EOF after "GO\n"+input_text (member reads 4 bytes then hands its pipe to the target, :146-159), so DEVNULL-equivalent — no hang. Side effect: text-mode pipe (:195) turns "\n" into "\r\n" in any input_text (the member expecting b"GO\r\n" at :150 proves the translation). |
| R-62 POSIX killpg after clean exit | CONFIRM [INFERRED] | process_tree.py:288-304 unconditional SIGTERM to pgid | ProcessLookupError swallowed; harmless on clean exit, kills stragglers otherwise. Not executed (Windows host). |
| R-84 exit_code=None classified as nonzero-exit | CONFIRM / NARROW [OBSERVED] | provider_cli_common.py:449, 460-468 | classify(None, "hello", "") -> FAILED basis "nonzero-exit". Reachable only from a runner returning rc=None without timed_out / spawn_error; every in-scope runner sets one of the flags (recon.py:713-717, provider_probe_session.py:531-540) and run_managed_process returns an int. Edge case; the basis label is wrong for None. |
| R-85 forbidden-flag guard name-matches values | CONFIRM [OBSERVED] | provider_cli_common.py:274-287 pairs EVERY token; 312-316 | `-m tools|agent|allow|worktree|yolo|-Tools` all REFUSED (ValueError). Fail-closed only (no bypass); `-m x=agents` and `--cwd D:\proj\--tools` pass. |
| R-61 / N-04 15 s per managed call | CONFIRM mechanism [OBSERVED] | process_tree.py:227 `terminate_and_wait(15.0)` on every exit; 125-132 `WaitForSingleObject(job, 15000)` even when `pids()` is empty (a job handle is only signalled by a job-time-limit) | Reproduced 15.2 s on a trivial python child. `-Action status` issues 0 managed calls (bare `subprocess_runner`, recon.py:707-717; 2 bare calls per provider; only 2 providers exist in that runner — `--provider all|grok|gemini`, recon.py:1433 / ps1 ValidateSet). Only `-Action probe` is managed: 1 per provider -> ~30 s tax for `-Provider all`. |
| ANTIGRAVITY_HEADLESS_MODE == "plan"; no antigravity_execution carve-out | CONFIRM [OBSERVED] | antigravity.py:78, 100, 138; provider_cli_common.py:295-319 has no execution_profile parameter; only tools/mutation/_op13_permission_mode_mutations.py mentions the carve-out (as a mutation) | Headless and interactive argv both pin `--mode plan`; the guard is unconditional. |
| Claude default-model path | CONFIRM [OBSERVED] | claude_code.py:398-401 (headless: no `--model` when self.model None), 331-334 (interactive: `[exe]` only); resolve_claude_model_ref(None) -> (None, note) :118-120 | Ledger `.sovereign_store/model_probe/claude_code.json`: fable-5 -> accepted_slug claude-fable-5, probed 2026-07-26T02:41:11Z. Honoured ONLY on the no-descriptor path (executed: `--model claude-fable-5`); bypassed on the production descriptor path (B-1). |

### B. New findings

B-1 | HIGH | [OBSERVED] | Production conductor-launch path bypasses the model-probe ledger and asserts `model_available: True` for a slug the host CLI is recorded rejecting
tools/live/emit_conductor_launch.py:532-533 always passes `descriptor=load_runtime_conductor_descriptor()`; inside build_conductor_launch_ticket :262-271 `desc is not None` sets `model = desc.model_id` (registry literal "fable-5" / "opus-4.8", control_plane/conductor/registry.py:92, 100) and `model_available = True`, so the ledger read at :293 (`model is None and model_available is None`) never runs. Executed with the real host ledger: argv `['...\claude.EXE','--model','fable-5']`, model_probe.source "registered-exact-slug". The same ledger records attempt 1 `--model fable-5` -> "There's an issue with the selected model (fable-5)" and `claude-fable-5` accepted. This regresses the 17A `.roundtrip` fix and mislabels an unverified label as an exact accepted slug. Precondition: conductor preference = Claude (config/live_operation.json currently prefers Codex gpt-5.6-sol; an absent `conductor` block -> registry.py:176-186 defaults to claude/fable-5). Tests cover only the no-descriptor path (tests/unit/test_emit_conductor_launch.py:405-442). Fix: consult `resolve_launch_model(desc.model_id)` on the descriptor path for claude_code, or store accepted CLI slugs in the registry.

B-2 | HIGH | [OBSERVED] | cp1252 decoding silently blanks or corrupts provider output on every non-recon spawn path
process_tree.py:195 (member Popen; used by claude_code.py:460, provider_cli_common.py:1201, provider_probe_session.py:529) and codex.py:219, 658 use `text=True` without `encoding=`; host preferred encoding is cp1252 and nothing sets PYTHONUTF8 (run_frontier_providers.ps1:231-250 sets PYTHONIOENCODING, which does not affect subprocess pipes). Executed: any UTF-8 sequence containing 0x81/0x8D/0x8F/0x90/0x9D as a continuation byte (U+276F, U+258x block glyphs, many emoji F0 9F 8D/8F/90 xx) kills the stdlib reader thread -> `stdout` None / '' with rc 0. Consequences by code: CodexCliBackend.generate returns `{"codex_exec":"empty"...}` as SUCCESS (:667-671) -> published CANDIDATE; CodexCli.version() -> CodexUnavailable "exited 0 with output ''" (:229-232), i.e. a present CLI reported ABSENT; ClaudeCliBackend.generate -> RuntimeError "non-JSON output: ''" after a SPENT call (:471-474); FrontierProviderCliBackend.generate returns "" as SUCCESS. Defined non-ASCII bytes ("—", "→", "✓", curly quotes) decode as mojibake and are published verbatim. Fix: `encoding="utf-8", errors="replace"` everywhere (mirror recon.py:712).

B-3 | HIGH | [OBSERVED] | Prompt / objective text reaches cmd.exe through the npm `.cmd` shim (widens the lead's R-14 from paths/slugs to node-controlled content; adds env expansion and newline truncation)
codex resolves to `...\npm\codex.CMD` on this host (shutil.which observed; codex.py:572-574, detect.py). CodexCliBackend.build_command appends the prompt as the last argv element (codex.py:621) and generate spawns via subprocess.run (:658). Executed against a scratch fakecodex.cmd that only echoes `%*`: prompt `Summarise Q3 report" & echo INJECTED2 & rem "` executed `INJECTED2` as a separate command; prompt `x %PATH:~0,5% y` arrived as `x C:\Us y` (env-var expansion INTO the model prompt); prompt `a\nb ...` arrived as `a` (silent truncation at the first newline). The prompt is `f"Task {task_id}. {objective}"` with objective = MCP context content (adapters/model_adapter.py:76-84, T2 node-controlled). Same shape for opencode.CMD (harness.py:141-149 positional prompt; driver.py:107) and latent for grok.CMD (`-p <prompt>`, generate unwired). claude.EXE / agy.EXE unaffected. Precondition: the live Codex worker path (codex_spawn) on a host where `codex` is the npm shim. Fix: spawn `node.exe <shim target .js>` instead of the .CMD, or refuse .cmd/.bat executables for argv carrying free text.

B-4 | MEDIUM | [OBSERVED] | Codex backend demotes a ZERO exit to an auth pause on stdout keywords
codex.py:667-670: after rc==0, `if self._classify(out) and not out.startswith("{")` -> CodexAuthError. `_AUTH_FAILURE_MARKERS` (:417-423) include "quota", "rate limit", "http 429", "session expired". Executed: replies "The API enforces a per-user quota of 100 req/min..." and "Handle HTTP 429 (rate limit) by exponential backoff." both -> PAUSE. `codex exec` prints plain text, so the "{" exemption never applies. A Codex reasoning worker asked anything about quotas/rate limits pauses the node fail-closed and the successful call is lost. Contradicts the §6 rule enforced in provider_cli_common.classify_provider_outcome:449-459. Fix: classify only on nonzero exit (or a structured error field).

B-5 | MEDIUM | [OBSERVED] | `agy models` parser accepts any space-free line as a model; the picker offers it as `verified=True`
provider_cli_common.py:934-941 keeps every line matching MODEL_SLUG_RE that has no space and no trailing ':'. Executed: "Unauthorized\n401\nrefreshing...\nplease-login\ngemini-3-pro" -> models ['Unauthorized','401','refreshing...','please-login','gemini-3-pro'], parse_note "ok". control_plane/nodes/pane_picker.py:353-370 turns each into a picker option with `verified=True` ("accepted-id verified"), and it flows to argv `--model Unauthorized` (worker_pane_spawn.py:565). Precondition: agy prints single-word diagnostics with exit 0 (the docstring itself anticipates "a banner, or an error paragraph"). Slugs are regex-clean, so no argv injection; it is a fabricated-inventory issue (§8). Fix: require a known prefix family / the models header, or reject inventories containing HTTP-status / auth words.

B-6 | LOW-MEDIUM | [OBSERVED] | Grok AUTHENTICATED can be minted by prose containing "you are logged in"
`_GROK_LOGIN_MARKER_RE` is `search`ed on every line (provider_cli_common.py:821, 886-891). Executed: "Tip: if you are logged in elsewhere, run `grok login` again." + a models section -> login_reported=True -> AUTH_AUTHENTICATED (:1047-1050) / recon :952-955 with exit 0. Answers "AVAILABLE/AUTHENTICATED from controlled stdout alone": yes for grok (prose + rc 0) and codex ("logged in" substring + rc 0, codex.py:245-247). Exit-code-first is respected (rc 0 required) but the positive predicate is loose. Fix: anchor with `^` and require the exact declarative sentence.

B-7 | LOW | [OBSERVED] | Codex live backend runs OUTSIDE the job-object boundary and lets TimeoutExpired escape raw
codex.py:658 uses bare `subprocess.run` (Claude uses run_managed_process, claude_code.py:460); on timeout only the direct child is killed, the codex.CMD -> cmd.exe -> node.exe chain can leave node.exe running, and `subprocess.TimeoutExpired` is not caught (:655-661) — ModelWorkerAdapter's generic except turns it into a task failure, so no crash, but no teardown accounting and no spawned_pids. Also CodexCli._run (:219). Fix: route through run_managed_process (accepting the 15 s tax or fixing R-61).

B-8 | LOW | [OBSERVED] | Text-mode stdin pipe rewrites input_text newlines to CRLF on the Windows managed path
process_tree.py:195 `text=True` on the member's stdin pipe; :219-220 writes "GO\n"+input_text; the member reads b"GO\r\n" (:150), proving translation. Any caller passing `input_text` gets CRLF-normalised stdin in the target. No in-scope caller passes input_text today (all pass DEVNULL, which is ignored per R-60), so latent. Fix: `newline=""` or a binary stdin pipe.

B-9 | INFO | [OBSERVED] | Guard-strictness asymmetry across adapters
The OP-12 guard normalises dashes and splits `=` (provider_cli_common.py:266-287); the Claude (claude_code.py:306-316) and Codex (codex.py:432-440) guards are exact lowercase-token matches: `--dangerously-skip-permissions=...`, `-dangerously-skip-permissions`, `--permission-mode bypassPermissions`, `--allowedTools`, `-a never`, `-c approval_policy=never` are NOT refused by the Claude/Codex guards (provider_commands.py:52-53 itself emits `--config ...`, so the config vector is open by design). Nothing in scope emits these; defence-in-depth only.

B-10 | INFO | [OBSERVED] | Recon and adapter disagree on which stream carries the Grok auth line
provider_cli_common.py:1035-1039 says stderr and merges the streams; recon.py:946 reads stdout only. One of the two is wrong for the installed CLI (ties to R-08); the recorded phase18a evidence should settle it.

Text-predicate inventory (strict vs loose): STRICT — parse_grok_models exact line "you are not authenticated." (:883), models header (:827), MODEL_SLUG_RE (:820), `_GROK_MODEL_ANNOTATION_RE` (:826), _SERVICE_NAME_RE (:830), codex `_NOT_LOGGED_IN_MARKERS` (codex.py:64), claude_model_probe exception-type-first classification (:97-104). LOOSE — `_GROK_LOGIN_MARKER_RE` search (B-6); parse_antigravity_models any space-free line (B-5); parse_version first triple anywhere in line 0 (R-48); AUTH_MARKERS / USAGE_MARKERS substring on merged lowercased streams (:400-409, nonzero-exit only — correct); codex `_LOGGED_IN_MARKERS ("logged in",)` substring (:63) and `_AUTH_FAILURE_MARKERS` incl. "quota" applied on ZERO exit (B-4); codex exec_help "--model" / "non-interactive" / "run codex" substrings (:294-295); claude `_AUTH_FAILURE_MARKERS` (claude_code.py:180-184, nonzero / is_error only — correct); claude_model_probe `_MODEL_UNAVAILABLE_MARKERS` applied to a zero-exit REPLY (:257 — deliberate, documented). CLI stdout that later reaches argv: only regex-clean model slugs from `grok models` / `agy models` via the picker (pane_picker.py:353-370 -> worker_pane_spawn.py:559-565); Claude `modelUsage` keys reach only the ledger `checkpoint` / chrome (never argv, paths or file names). No CLI text builds a path or file name in scope.

### C. What you executed
- `py -3.12 -m pytest tests/unit/test_frontier_provider_recon.py tests/unit/test_run_frontier_providers_ps1.py -q -p no:cacheprovider` -> 249 passed, 1 skipped (host-coupled, SOW_PROVIDER_HOST_CHECK unset), 8.93 s.
- scratch f3_probe.py: build_conductor_launch_ticket with injected auth / lease ledger / cli_present=True: (a) empty ledger + "opus-4.8"; (b) host ledger, no descriptor; (c) host ledger + the Claude descriptor as main() would pass -> A (R-25) and B-1. No CLI spawned.
- scratch pure_checks.py: parse_version cases; assert_no_forbidden_provider_args value-token cases; classify_provider_outcome(None, ...); R-24 detail vs outcome; parse_grok_models stdout vs merged; 41-name scrub matrix across the three classifiers.
- scratch enc_probe.py: python child emitting UTF-8 with 0x9D via subprocess.run(text=True) and via run_managed_process (15.2 s) -> B-2.
- scratch cmd_inj.py / cmd_inj2.py: CodexCliBackend.build_command run against a scratch echo-only fakecodex.cmd -> B-3.
- scratch pred_checks.py: grok loose login regex, agy parser, codex zero-exit demotion -> B-4 / B-5 / B-6.
- shutil.which resolution of claude / codex / grok / agy / opencode on this host (no execution); locale.getpreferredencoding(False) -> cp1252; PYTHONUTF8 unset.

### D. What is genuinely strong in this area
- `classify_provider_outcome` (provider_cli_common.py:431-468) is genuinely exit-code-first: rc 0 is never demoted by text, only by a structured error field; `basis` makes the rule auditable in evidence.
- The OP-12 argv guard (`_flag_pairs`, :266-319) is dash-normalised, `=`-split and unconditional (no execution_profile carve-out); refusals are loud ValueErrors and tests pin the absence of the keyword.
- MODEL_SLUG_RE (:820) excludes every cmd.exe metacharacter, so listing-derived slugs cannot become the R-14 / B-3 vector even through .cmd shims.
- ModelProbeLedger fails closed to UNPROBED on any shape drift, refuses self-contradictory records and writes atomically (claude_model_probe.py:157-196, 358-373); `verify_reported_checkpoint` requires exact type + spent call + datable checkpoint (claude_code.py:568-613).
- The recon runner (recon.py:707-717) already does the right thing on all four axes: utf-8 + errors=replace, DEVNULL stdin, scrubbed env, five-tuple never-raise contract — it is the template the other spawn sites should copy.
- Credential policy is one object re-exported (recon.py:192-259) with test-existence pinning `reads_credentials_by_design`; the PRESERVED list is a single evidenced key (GROK_SANDBOX).

### E. Not covered / open questions
- adapters/local/ (ollama_session.py, worker.py) and adapters/coding/opencode beyond spawn shape were not reviewed; accept_probe / _token_answered echo-subtraction (recon.py:388-530) not audited.
- Whether the installed grok CLI writes its auth line to stdout or stderr was not observed (no CLI invoked) — B-10 records the disagreement, not its resolution.
- POSIX branch of process_tree not executed (Windows host).
- run_frontier_providers.ps1 install / login branches only read for call counts.
- Whether claude.EXE honours NODE_OPTIONS at all is unknown — the R-42 leak matters for the npm-shim CLIs (codex / grok / opencode) more than for claude.EXE / agy.EXE.
